---
phase: 01-unity-6-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Resources/Particle/Script/Wind.cs
  - Assets/Resources/Particle/Script/ExplosionObject.cs
  - Assets/Resources/Particle/Script/ParticleSetting.cs
  - Assets/Resources/Particle/Script/Slash.cs
  - Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.cs
  - Assets/Resources/Prefab/Particle/150316/Scripts/MGE_SetInitialForces.cs
  - Assets/Resources/Prefab/Particle/140520/Scripts/SM_trailFade.cs
  - Assets/Resources/Prefab/Particle/140520/Scripts/SM_rotateThis.cs
  - Assets/Resources/Prefab/Particle/yanwei/shader/randomRotate.cs
  - ProjectSettings/EditorBuildSettings.asset
  - .planning/phases/01-unity-6-migration/scene-list-backup.txt
autonomous: true
requirements:
  - ENV-01

must_haves:
  truths:
    - "No .js (UnityScript) files exist under Assets/"
    - "C# equivalents exist for all 9 particle/utility scripts that were .js"
    - "SqliteAccess.js deleted (C# counterpart SqliteAccessCS.cs already exists)"
    - "ProjectSettings .asset files deleted (except ProjectVersion.txt)"
    - "Scene list preserved in a backup file before ProjectSettings deletion"
  artifacts:
    - path: "Assets/Resources/Particle/Script/Wind.cs"
      provides: "C# replacement for Wind.js"
      contains: "class Wind"
    - path: "Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.cs"
      provides: "C# replacement for MGE_destroyThisTimed.js"
      contains: "class MGE_destroyThisTimed"
    - path: ".planning/phases/01-unity-6-migration/scene-list-backup.txt"
      provides: "Backup of EditorBuildSettings scene list"
      contains: "GameStart"
  key_links:
    - from: "Assets/Resources/Particle/Script/Wind.cs"
      to: "Unity MonoBehaviour"
      via: "class inheritance"
      pattern: "MonoBehaviour"
---

<objective>
Remove UnityScript files and regenerate ProjectSettings for Unity 6 compatibility.

Purpose: Unity 6 cannot parse .js (UnityScript) files at all -- they will block project import. Binary ProjectSettings from Unity 5.2.4f1 will corrupt or crash the Unity 6 editor. Both must be dealt with BEFORE opening the project in Unity 6.
Output: 10 .js files deleted (9 replaced with C# equivalents, 1 deleted with existing C# counterpart), ProjectSettings cleaned for Unity 6 regeneration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unity-6-migration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert UnityScript files to C# and delete originals</name>
  <files>
    Assets/Resources/Particle/Script/Wind.js
    Assets/Resources/Particle/Script/Wind.cs
    Assets/Resources/Particle/Script/ExplosionObject.js
    Assets/Resources/Particle/Script/ExplosionObject.cs
    Assets/Resources/Particle/Script/ParticleSetting.js
    Assets/Resources/Particle/Script/ParticleSetting.cs
    Assets/Resources/Particle/Script/Slash.js
    Assets/Resources/Particle/Script/Slash.cs
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.js
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.cs
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_SetInitialForces.js
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_SetInitialForces.cs
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_trailFade.js
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_trailFade.cs
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_rotateThis.js
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_rotateThis.cs
    Assets/Resources/Prefab/Particle/yanwei/shader/randomRotate.js
    Assets/Resources/Prefab/Particle/yanwei/shader/randomRotate.cs
    Assets/Scripts/Database/SqliteAccess.js
  </files>
  <read_first>
    Assets/Resources/Particle/Script/Wind.js
    Assets/Resources/Particle/Script/ExplosionObject.js
    Assets/Resources/Particle/Script/ParticleSetting.js
    Assets/Resources/Particle/Script/Slash.js
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.js
    Assets/Resources/Prefab/Particle/150316/Scripts/MGE_SetInitialForces.js
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_trailFade.js
    Assets/Resources/Prefab/Particle/140520/Scripts/SM_rotateThis.js
    Assets/Resources/Prefab/Particle/yanwei/shader/randomRotate.js
    Assets/Scripts/Database/SqliteAccess.js
    Assets/Scripts/Database/SqliteAccessCS.cs
  </read_first>
  <action>
Convert each of the 9 particle/utility .js files to C# equivalents. The conversions are mechanical -- UnityScript to C# syntax changes:

**Wind.js -> Wind.cs:**
```csharp
using UnityEngine;
public class Wind : MonoBehaviour
{
    public Vector3 speed;
    public Vector3 speedRedirect;
    void Start()
    {
        speed += new Vector3(
            Random.Range(-speedRedirect.x, speedRedirect.x),
            Random.Range(-speedRedirect.y, speedRedirect.y),
            Random.Range(-speedRedirect.z, speedRedirect.z));
    }
    void Update()
    {
        GetComponent<Rigidbody>().velocity += speed * Time.deltaTime;
    }
}
```

**MGE_destroyThisTimed.js -> MGE_destroyThisTimed.cs:**
```csharp
using UnityEngine;
public class MGE_destroyThisTimed : MonoBehaviour
{
    public float destroyTime = 5f;
    void Start() { Destroy(gameObject, destroyTime); }
}
```

**MGE_SetInitialForces.js -> MGE_SetInitialForces.cs:**
```csharp
using UnityEngine;
public class MGE_SetInitialForces : MonoBehaviour
{
    public bool relativeForce = true;
    public float x, xDeviation;
    public float y, yDeviation;
    public float z, zDeviation;
    public bool relativeTorque = true;
    public float torqueScale = 100f;
    public float xRot, xRotDeviation;
    public float yRot, yRotDeviation;
    public float zRot, zRotDeviation;

    void Start()
    {
        var rb = GetComponent<Rigidbody>();
        var force = new Vector3(
            Random.Range(x - xDeviation, x + xDeviation),
            Random.Range(y - yDeviation, y + yDeviation),
            Random.Range(z - zDeviation, z + zDeviation));
        if (relativeForce) rb.AddRelativeForce(force);
        else rb.AddForce(force);

        var torque = new Vector3(
            Random.Range(xRot - xRotDeviation, xRot + xRotDeviation) * torqueScale,
            Random.Range(yRot - yRotDeviation, yRot + yRotDeviation) * torqueScale,
            Random.Range(zRot - zRotDeviation, zRot + zRotDeviation) * torqueScale);
        if (relativeTorque) rb.AddRelativeTorque(torque);
        else rb.AddTorque(torque);
    }
}
```

**SM_trailFade.js -> SM_trailFade.cs:**
```csharp
using UnityEngine;
public class SM_trailFade : MonoBehaviour
{
    public float fadeInTime = 0.1f;
    public float stayTime = 1f;
    public float fadeOutTime = 0.7f;
    public TrailRenderer thisTrail;
    private float timeElapsed;
    private float timeElapsedLast;
    private float percent;

    void Start()
    {
        thisTrail.material.SetColor("_TintColor", new Color(0.5f, 0.5f, 0.5f, 1f));
        if (fadeInTime < 0.01f) fadeInTime = 0.01f;
        percent = timeElapsed / fadeInTime;
    }

    void Update()
    {
        timeElapsed += Time.deltaTime;
        if (timeElapsed <= fadeInTime)
        {
            percent = timeElapsed / fadeInTime;
            thisTrail.material.SetColor("_TintColor", new Color(0.5f, 0.5f, 0.5f, percent));
        }
        else if (timeElapsed < fadeInTime + stayTime)
        {
            thisTrail.material.SetColor("_TintColor", new Color(0.5f, 0.5f, 0.5f, 1f));
        }
        else if (timeElapsed < fadeInTime + stayTime + fadeOutTime)
        {
            timeElapsedLast += Time.deltaTime;
            percent = 1f - (timeElapsedLast / fadeOutTime);
            thisTrail.material.SetColor("_TintColor", new Color(0.5f, 0.5f, 0.5f, percent));
        }
    }
}
```

**SM_rotateThis.js -> SM_rotateThis.cs:**
```csharp
using UnityEngine;
public class SM_rotateThis : MonoBehaviour
{
    public float rotationSpeedX = 90f;
    public float rotationSpeedY;
    public float rotationSpeedZ;
    public bool local = true;

    void Update()
    {
        var rot = new Vector3(rotationSpeedX, rotationSpeedY, rotationSpeedZ) * Time.deltaTime;
        if (local) transform.Rotate(rot);
        else transform.Rotate(rot, Space.World);
    }
}
```

**randomRotate.js -> randomRotate.cs:**
```csharp
using UnityEngine;
public class randomRotate : MonoBehaviour
{
    public float rotateEverySecond = 1f;
    private Quaternion rotTarget;

    void Start()
    {
        RandomRot();
        InvokeRepeating(nameof(RandomRot), 0f, rotateEverySecond);
    }

    void Update()
    {
        transform.rotation = Quaternion.Lerp(transform.rotation, rotTarget, Time.time * Time.deltaTime);
    }

    void RandomRot()
    {
        rotTarget = Random.rotation;
    }
}
```

**ExplosionObject.js -> ExplosionObject.cs:**
```csharp
using UnityEngine;
public class ExplosionObject : MonoBehaviour
{
    public Vector3 Force;
    public GameObject Objcet; // preserve original typo for prefab reference compatibility
    public int Num;
    public int Scale = 20;
    public int ScaleMin = 10;
    public AudioClip[] Sounds;
    public float LifeTimeObject = 2f;
    public float postitionoffset = 2f; // preserve original typo
    public bool random;

    void Start()
    {
        Physics.IgnoreLayerCollision(2, 2);
        if (Sounds.Length > 0)
            AudioSource.PlayClipAtPoint(Sounds[Random.Range(0, Sounds.Length)], transform.position);
        if (Objcet != null)
        {
            int num = random ? Random.Range(1, Num) : Num;
            for (int i = 0; i < num; i++)
            {
                var pos = new Vector3(
                    Random.Range(-postitionoffset, postitionoffset),
                    Random.Range(-postitionoffset, postitionoffset),
                    Random.Range(-postitionoffset, postitionoffset)) / 10f;
                var obj = Instantiate(Objcet, transform.position + pos, Random.rotation);
                Destroy(obj, LifeTimeObject);
                if (Scale > 0)
                {
                    float scale = Random.Range(ScaleMin, Scale);
                    obj.transform.localScale = new Vector3(scale, scale, scale);
                }
                var rb = obj.GetComponent<Rigidbody>();
                if (rb != null)
                    rb.AddForce(new Vector3(
                        Random.Range(-Force.x, Force.x),
                        Random.Range(-Force.y, Force.y),
                        Random.Range(-Force.z, Force.z)));
            }
        }
    }
}
```

**ParticleSetting.js -> ParticleSetting.cs:**
```csharp
using UnityEngine;
public class ParticleSetting : MonoBehaviour
{
    public float LightIntensityMult = -0.5f;
    public float LifeTime = 1f;
    public bool RandomRotation;
    public Vector3 PositionOffset;
    public GameObject SpawnEnd;
    private float timetemp;

    void Start()
    {
        timetemp = Time.time;
        if (RandomRotation)
        {
            transform.rotation = Random.rotation;
        }
    }

    void Update()
    {
        if (Time.time > timetemp + LifeTime)
        {
            if (SpawnEnd != null)
                Instantiate(SpawnEnd, transform.position, transform.rotation);
            Destroy(gameObject);
        }
        var light = GetComponent<Light>();
        if (light != null)
            light.intensity += LightIntensityMult * Time.deltaTime;
    }
}
```

**Slash.js -> Slash.cs:**
```csharp
using UnityEngine;
public class Slash : MonoBehaviour
{
    public Vector3 speed = Vector3.one;

    void Update()
    {
        var ls = transform.localScale;
        ls.x += speed.x * Time.deltaTime;
        ls.y += speed.y * Time.deltaTime;
        if (ls.y < 0f) ls.y = 0f;
        transform.localScale = ls;
    }
}
```

**SqliteAccess.js:** Delete this file only (do NOT create a C# replacement). The C# counterpart `SqliteAccessCS.cs` already exists in the same directory.

After creating all 9 .cs files, delete all 10 .js files. Also delete corresponding .js.meta files.

IMPORTANT: Preserve original field names including typos (e.g., `Objcet`, `postitionoffset`) because Unity prefabs reference fields by name. Changing names would break serialized references.
  </action>
  <verify>
    <automated>find Assets/ -name "*.js" -not -name "*.json" 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find Assets/ -name "*.js" -not -name "*.json" | wc -l` returns 0
    - `grep -l "class Wind : MonoBehaviour" Assets/Resources/Particle/Script/Wind.cs` returns the file path
    - `grep -l "class MGE_destroyThisTimed : MonoBehaviour" Assets/Resources/Prefab/Particle/150316/Scripts/MGE_destroyThisTimed.cs` returns the file path
    - `grep -l "class SM_trailFade : MonoBehaviour" Assets/Resources/Prefab/Particle/140520/Scripts/SM_trailFade.cs` returns the file path
    - `grep -l "class randomRotate : MonoBehaviour" Assets/Resources/Prefab/Particle/yanwei/shader/randomRotate.cs` returns the file path
    - `grep -l "class ExplosionObject : MonoBehaviour" Assets/Resources/Particle/Script/ExplosionObject.cs` returns the file path
    - `grep -l "class ParticleSetting : MonoBehaviour" Assets/Resources/Particle/Script/ParticleSetting.cs` returns the file path
    - `grep -l "class Slash : MonoBehaviour" Assets/Resources/Particle/Script/Slash.cs` returns the file path
    - `grep -l "class MGE_SetInitialForces : MonoBehaviour" Assets/Resources/Prefab/Particle/150316/Scripts/MGE_SetInitialForces.cs` returns the file path
    - `grep -l "class SM_rotateThis : MonoBehaviour" Assets/Resources/Prefab/Particle/140520/Scripts/SM_rotateThis.cs` returns the file path
    - `test ! -f Assets/Scripts/Database/SqliteAccess.js` exits 0
    - `test -f Assets/Scripts/Database/SqliteAccessCS.cs` exits 0
  </acceptance_criteria>
  <done>All 10 .js files deleted. 9 C# replacements created with matching class names and public field names. SqliteAccess.js deleted (C# counterpart already exists). Zero UnityScript files remain under Assets/.</done>
</task>

<task type="auto">
  <name>Task 2: Clean ProjectSettings for Unity 6 regeneration</name>
  <files>
    ProjectSettings/AudioManager.asset
    ProjectSettings/DynamicsManager.asset
    ProjectSettings/EditorBuildSettings.asset
    ProjectSettings/EditorSettings.asset
    ProjectSettings/GraphicsSettings.asset
    ProjectSettings/InputManager.asset
    ProjectSettings/NavMeshAreas.asset
    ProjectSettings/NavMeshLayers.asset
    ProjectSettings/NetworkManager.asset
    ProjectSettings/Physics2DSettings.asset
    ProjectSettings/ProjectSettings.asset
    ProjectSettings/QualitySettings.asset
    ProjectSettings/TagManager.asset
    ProjectSettings/TimeManager.asset
    ProjectSettings/UnityAdsSettings.asset
    ProjectSettings/UnityAnalyticsManager.asset
    ProjectSettings/ProjectVersion.txt
    .planning/phases/01-unity-6-migration/scene-list-backup.txt
  </files>
  <read_first>
    ProjectSettings/EditorBuildSettings.asset
    ProjectSettings/ProjectVersion.txt
  </read_first>
  <action>
1. Create a scene list backup file at `.planning/phases/01-unity-6-migration/scene-list-backup.txt` with this content (extracted from the binary EditorBuildSettings.asset):
```
# Scene list from original EditorBuildSettings.asset (Unity 5.2.4f1)
# Re-add these in Unity 6 via File > Build Settings after first project open
# Order matters -- index 0 is the default scene loaded on startup

0: Assets/Scenes/GameStart.unity
1: Assets/Scenes/GameRoleCustom.unity
2: Assets/Scenes/GameMainMenu.unity
3: Assets/Scenes/GameLobby.unity
4: Assets/Scenes/MLoginScene.unity
5: Assets/Scenes/PeGame.unity
6: Assets/Scenes/Intro.unity
7: Assets/Scenes/GameTheEnd.unity
8: Assets/Scenes/GameCredits.unity
9: Assets/Scenes/GameED.unity
```

2. Delete ALL .asset files in ProjectSettings/ directory. These are binary files from Unity 5.2.4f1 that are not forward-compatible with Unity 6. The list to delete:
   - AudioManager.asset
   - DynamicsManager.asset
   - EditorBuildSettings.asset
   - EditorSettings.asset
   - GraphicsSettings.asset
   - InputManager.asset
   - NavMeshAreas.asset
   - NavMeshLayers.asset
   - NetworkManager.asset
   - Physics2DSettings.asset
   - ProjectSettings.asset
   - QualitySettings.asset
   - TagManager.asset
   - TimeManager.asset
   - UnityAdsSettings.asset
   - UnityAnalyticsManager.asset

3. Keep ProjectSettings/ProjectVersion.txt (Unity reads this to know what version the project was last opened with). Do NOT delete it.

4. Also delete any .asset.meta files in ProjectSettings/ if they exist.

Unity 6 will regenerate all deleted .asset files with correct defaults on first project open.
  </action>
  <verify>
    <automated>ls ProjectSettings/*.asset 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls ProjectSettings/*.asset 2>/dev/null | wc -l` returns 0
    - `test -f ProjectSettings/ProjectVersion.txt` exits 0
    - `cat ProjectSettings/ProjectVersion.txt` contains "5.2.4f1"
    - `test -f .planning/phases/01-unity-6-migration/scene-list-backup.txt` exits 0
    - `grep "GameStart" .planning/phases/01-unity-6-migration/scene-list-backup.txt` matches
    - `grep "PeGame" .planning/phases/01-unity-6-migration/scene-list-backup.txt` matches
    - Scene list contains exactly 10 entries (lines 0-9)
  </acceptance_criteria>
  <done>All binary .asset files deleted from ProjectSettings/. ProjectVersion.txt preserved. Scene list backed up with all 10 scenes documented in order. Unity 6 will regenerate default settings on first open.</done>
</task>

</tasks>

<verification>
- `find Assets/ -name "*.js" -not -name "*.json" | wc -l` returns 0 (no UnityScript files remain)
- All 9 C# replacement files exist and contain `MonoBehaviour` class declarations
- `ls ProjectSettings/*.asset 2>/dev/null | wc -l` returns 0 (all binary settings deleted)
- `ProjectSettings/ProjectVersion.txt` exists and is intact
- Scene list backup exists at `.planning/phases/01-unity-6-migration/scene-list-backup.txt`
</verification>

<success_criteria>
Zero UnityScript files under Assets/. Nine C# replacements with matching class names and field names. ProjectSettings cleaned to only ProjectVersion.txt. Scene list preserved in planning directory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-unity-6-migration/01-01-SUMMARY.md`
</output>
