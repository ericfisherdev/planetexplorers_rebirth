---
phase: 02-proprietary-plugin-stubs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Stubs/FinalIK/AimIK.cs
  - Assets/Stubs/FinalIK/FullBodyBipedIK.cs
  - Assets/Stubs/FinalIK/CCDIK.cs
  - Assets/Stubs/FinalIK/GrounderFBBIK.cs
  - Assets/Stubs/FinalIK/IKSolvers.cs
  - Assets/Stubs/FinalIK/IKTypes.cs
  - Assets/Stubs/AstarPathfinding/AstarPath.cs
  - Assets/Stubs/AstarPathfinding/Seeker.cs
  - Assets/Stubs/AstarPathfinding/Path.cs
  - Assets/Stubs/AstarPathfinding/RandomPath.cs
  - Assets/Stubs/AstarPathfinding/GraphTypes.cs
  - Assets/Stubs/AstarPathfinding/RVO.cs
  - Assets/Stubs/AstarPathfinding/Ionic.cs
  - Assets/Stubs/Behave/BehaveRuntime.cs
  - Assets/Stubs/FMOD/FMODAudioSource.cs
  - Assets/Stubs/FMOD/FMODAudioListener.cs
  - Assets/Stubs/FMOD/FMODStudio.cs
  - Assets/Stubs/FMOD/FMODAsset.cs
  - Assets/Stubs/FMOD/FMOD_StudioSystem.cs
autonomous: true
requirements:
  - ENV-02

must_haves:
  truths:
    - "All files using RootMotion.FinalIK types (AimIK, FullBodyBipedIK, CCDIK, GrounderFBBIK) compile"
    - "All files using Pathfinding namespace types (AstarPath, Seeker, Path, RandomPath, GridGraph) compile"
    - "All files using Behave.Runtime types (BehaveResult, IAgent, Tree) compile"
    - "All files using FMOD/FMODUnity types compile"
  artifacts:
    - path: "Assets/Stubs/FinalIK/AimIK.cs"
      provides: "AimIK MonoBehaviour stub with solver property"
      contains: "namespace RootMotion.FinalIK"
    - path: "Assets/Stubs/AstarPathfinding/AstarPath.cs"
      provides: "AstarPath singleton stub"
      contains: "namespace Pathfinding"
    - path: "Assets/Stubs/Behave/BehaveRuntime.cs"
      provides: "BehaveResult enum, IAgent interface, Tree class"
      contains: "namespace Behave.Runtime"
    - path: "Assets/Stubs/FMOD/FMODAudioSource.cs"
      provides: "FMODAudioSource MonoBehaviour stub"
      contains: "public class FMODAudioSource"
  key_links:
    - from: "Assets/Scripts/Assist/IKCombat.cs"
      to: "Assets/Stubs/FinalIK/"
      via: "using RootMotion.FinalIK namespace"
      pattern: "using RootMotion\\.FinalIK"
    - from: "Assets/Scripts/Assist/PEPathfinder.cs"
      to: "Assets/Stubs/AstarPathfinding/"
      via: "using Pathfinding namespace"
      pattern: "using Pathfinding"
    - from: "Assets/Scripts/BehaviorTree/Scripts/BTAgent.cs"
      to: "Assets/Stubs/Behave/"
      via: "using Behave.Runtime namespace"
      pattern: "Behave\\.Runtime\\.Tree|BehaveResult|IAgent"
---

<objective>
Create stub source files for FinalIK, A* Pathfinding Project, Behave behavior tree runtime, and FMOD audio -- the gameplay system plugins. These are smaller than NGUI/uLink but critical for AI, animation, pathfinding, and audio compilation.

Purpose: Eliminate all CS0246 missing-type errors for gameplay middleware. FinalIK (71 AimIK + 15 FullBodyBipedIK refs), A* Pathfinding (55 AstarPath refs), Behave (2110 BehaveResult refs), and FMOD (~24 FMODAudioSource refs) all need stub types to compile.

Output: Assets/Stubs/FinalIK/, Assets/Stubs/AstarPathfinding/, Assets/Stubs/Behave/, Assets/Stubs/FMOD/ directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

KEY CONTEXT:
- Behave has significant IN-HOUSE code already in Assets/Scripts/BehaviorTree/Scripts/ (BTAgent, BTResolver, BTLauncher, BTAction, BehaveAction attribute, BehaveAttribute). The stubs only need to provide: BehaveResult enum (Success, Failure, Running), IAgent interface, and Tree class -- the proprietary runtime types.
- A* Pathfinding: game uses Pathfinding.RVO which is Pro-only. Stub RVO as no-op.
- FinalIK: .solver property on IK components is heavily accessed (IKPositionWeight, IKPosition, iterations, axis, target, etc.)
- FMOD: FMODAudioSource and FMODAudioListener are MonoBehaviours. FMOD.Studio namespace has EventDescription, Bus, VCA, Bank, PARAMETER_DESCRIPTION.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinalIK and A* Pathfinding stubs</name>
  <files>
    Assets/Stubs/FinalIK/AimIK.cs,
    Assets/Stubs/FinalIK/FullBodyBipedIK.cs,
    Assets/Stubs/FinalIK/CCDIK.cs,
    Assets/Stubs/FinalIK/GrounderFBBIK.cs,
    Assets/Stubs/FinalIK/IKSolvers.cs,
    Assets/Stubs/FinalIK/IKTypes.cs,
    Assets/Stubs/AstarPathfinding/AstarPath.cs,
    Assets/Stubs/AstarPathfinding/Seeker.cs,
    Assets/Stubs/AstarPathfinding/Path.cs,
    Assets/Stubs/AstarPathfinding/RandomPath.cs,
    Assets/Stubs/AstarPathfinding/GraphTypes.cs,
    Assets/Stubs/AstarPathfinding/RVO.cs,
    Assets/Stubs/AstarPathfinding/Ionic.cs
  </files>
  <read_first>
    Discover exact API surfaces:
    - `grep -rn 'AimIK\.' Assets/Scripts/ | grep -oP 'AimIK\.\w+\|\.solver\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'FullBodyBipedIK\.' Assets/Scripts/ | grep -oP 'FullBodyBipedIK\.\w+\|\.solver\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'CCDIK\.' Assets/Scripts/ | grep -oP 'CCDIK\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'GrounderFBBIK' Assets/Scripts/ | head -15`
    - `grep -rn 'IKSolverFullBodyBiped\.' Assets/Scripts/ | grep -oP 'IKSolverFullBodyBiped\.\w+\|\.solver\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'IKEffector\|IKSolverAim\|IKSolverCCD\|IKSolverLookAt\|IKSolver\b' Assets/Scripts/ | grep -oP '\b(IKEffector|IKSolverAim|IKSolverCCD|IKSolverLookAt|IKSolver)\b' | sort | uniq -c | sort -rn`
    - `grep -rn 'AstarPath\.' Assets/Scripts/ | grep -oP 'AstarPath\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'Seeker\.' Assets/Scripts/ | grep -oP 'Seeker\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'ABPath\.\|\.path\b' Assets/Scripts/Assist/PEPathfinder.cs | head -20`
    - `grep -rn 'Pathfinding\.RandomPath\.' Assets/Scripts/ | grep -oP 'RandomPath\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'GridGraph\.\|LayerGridGraph\.' Assets/Scripts/ | head -15`
    - `grep -rn 'GraphNode\.' Assets/Scripts/ | head -10`
    - `grep -rn 'RVOController\|Pathfinding\.RVO\.' Assets/Scripts/ | head -10`
    - `grep -rn 'Pathfinding\.Ionic\|Ionic\.Zip' Assets/Scripts/ | head -10`
    - Read Assets/Scripts/Assist/PEPathfinder.cs (first 80 lines) for Seeker/Path usage patterns
    - Read Assets/Scripts/PeEntity/Cmpt/IKCmpt.cs for IK component setup pattern
    - Read Assets/Scripts/Assist/IKCombat.cs for CCDIK usage pattern
  </read_first>
  <action>
    Create Assets/Stubs/FinalIK/ and Assets/Stubs/AstarPathfinding/ directories.

    **FinalIK stubs** (all in `namespace RootMotion.FinalIK`):

    1. `IKTypes.cs` -- Base types and solver interfaces:
       - `IK` : MonoBehaviour (base class with `solver` property returning IKSolver, `enabled` bool, `GetIKSolver()` method)
       - `IKSolver` class (base solver with `IKPosition` Vector3, `IKPositionWeight` float, `SetIKPositionWeight()`, `SetIKPosition()`, `target` Transform, `transform` Transform, `Update()`, `OnPreUpdate` delegate, `axis` Vector3)
       - `IKSolverFullBodyBiped` : IKSolver (has `iterations` int, `leftArmMapping`, `rightArmMapping` IKMapping, `IKPositionWeight`, `clampSmoothing`, `maxFootRotationAngle`)
       - `IKMapping` class (empty stub)
       - `IKSolverAim` : IKSolver (has `axis` Vector3, `target` Transform, `IKPositionWeight`)
       - `IKSolverCCD` : IKSolver (empty)
       - `IKEffector` class (has `target` Transform, `positionWeight` float)

    2. `AimIK.cs` -- `class AimIK : IK` with `solver` returning IKSolverAim, `enabled` bool

    3. `FullBodyBipedIK.cs` -- `class FullBodyBipedIK : IK` with `solver` returning IKSolverFullBodyBiped

    4. `CCDIK.cs` -- `class CCDIK : IK` with `solver` returning IKSolverCCD

    5. `GrounderFBBIK.cs` -- `class GrounderFBBIK : MonoBehaviour` (ground alignment stub)

    6. `IKSolvers.cs` -- Additional solver types if needed after grep analysis

    **A* Pathfinding stubs** (all in `namespace Pathfinding`):

    1. `AstarPath.cs` -- `class AstarPath : MonoBehaviour`
       - `static active` property (singleton), `graphs` array of NavGraph, `GetNearest()`, `Scan()`, `FloodFill()` no-ops
       - `astarData` property

    2. `Seeker.cs` -- `class Seeker : MonoBehaviour`
       - `StartPath()`, `GetCurrentPath()`, `pathCallback` delegate
       - `OnPathDelegate` delegate type

    3. `Path.cs` -- `class Path`
       - `vectorPath` List<Vector3>, `error` bool, `errorLog` string, `Claim()`, `Release()`, `GetTotalLength()`
       - `class ABPath : Path` with `Construct()` static, `endPoint`, `startPoint`
       - `class NNConstraint` with `Default` static, `None` static

    4. `RandomPath.cs` -- `class RandomPath : ABPath`
       - `static Construct(Vector3 start, int length, OnPathDelegate callback)` returns new RandomPath
       - `aim` Vector3, `spread` int

    5. `GraphTypes.cs` -- Graph infrastructure:
       - `class NavGraph` (base, has `nodes`)
       - `class GridGraph : NavGraph` (has `width`, `depth`, `nodeSize`, `center`)
       - `class LayerGridGraph : GridGraph`
       - `class GraphNode` (has `position`, `Walkable`, `Tag`, `Penalty`)
       - `class NNInfo` struct (has `node`, `position`, `clampedPosition`)
       - `class PathNNConstraint` (has `Default` static)

    6. `RVO.cs` -- `namespace Pathfinding.RVO` containing:
       - `class RVOController : MonoBehaviour` (no-op)
       - Any other RVO types referenced by game code

    7. `Ionic.cs` -- `namespace Pathfinding.Ionic` (and `Pathfinding.Ionic.Zip` if needed):
       - The game uses `using Ionic = Pathfinding.Ionic;` in Zip.cs
       - Provide whatever Zip types are referenced (ZipFile, ZipEntry, etc.)
  </action>
  <verify>
    <automated>ls Assets/Stubs/FinalIK/*.cs Assets/Stubs/AstarPathfinding/*.cs 2>/dev/null | wc -l</automated>
    Expected: 13 files (6 FinalIK + 7 A* Pathfinding)
  </verify>
  <acceptance_criteria>
    - `grep -l "class AimIK" Assets/Stubs/FinalIK/AimIK.cs` returns the file
    - `grep -l "class FullBodyBipedIK" Assets/Stubs/FinalIK/FullBodyBipedIK.cs` returns the file
    - `grep -l "class CCDIK" Assets/Stubs/FinalIK/CCDIK.cs` returns the file
    - `grep -l "class AstarPath" Assets/Stubs/AstarPathfinding/AstarPath.cs` returns the file
    - `grep -l "class Seeker" Assets/Stubs/AstarPathfinding/Seeker.cs` returns the file
    - `grep -l "class RandomPath" Assets/Stubs/AstarPathfinding/RandomPath.cs` returns the file
    - `grep -l "namespace RootMotion.FinalIK" Assets/Stubs/FinalIK/AimIK.cs` returns the file
    - `grep -l "namespace Pathfinding" Assets/Stubs/AstarPathfinding/AstarPath.cs` returns the file
    - `grep -l "IKSolverFullBodyBiped" Assets/Stubs/FinalIK/IKTypes.cs` returns the file
    - `grep -l "IKPositionWeight\|IKPosition\|iterations" Assets/Stubs/FinalIK/IKTypes.cs` returns the file
  </acceptance_criteria>
  <done>FinalIK stubs provide all IK component types with solver properties matching the API surface used by game code. A* Pathfinding stubs provide AstarPath singleton, Seeker, Path types, RandomPath, graph types, and RVO no-ops.</done>
</task>

<task type="auto">
  <name>Task 2: Create Behave runtime and FMOD stubs</name>
  <files>
    Assets/Stubs/Behave/BehaveRuntime.cs,
    Assets/Stubs/FMOD/FMODAudioSource.cs,
    Assets/Stubs/FMOD/FMODAudioListener.cs,
    Assets/Stubs/FMOD/FMODStudio.cs,
    Assets/Stubs/FMOD/FMODAsset.cs,
    Assets/Stubs/FMOD/FMOD_StudioSystem.cs
  </files>
  <read_first>
    - Read Assets/Scripts/BehaviorTree/Scripts/BTAgent.cs (full file) to see Tree API used (Tick, Reset, Frequency, LibraryActions, LastTickedAction, Result)
    - Read Assets/Scripts/BehaviorTree/Scripts/BTInterface.cs to see IAgent interface usage
    - `grep -rn 'Tree\.' Assets/Scripts/BehaviorTree/ | grep -oP '(?:m_Tree|Tree)\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'IAgent\.' Assets/Scripts/ | grep -oP 'IAgent\.\w+\|agent\.\w+' | sort | uniq -c | sort -rn | head -10`
    - `grep -rn 'Behave\.Runtime\.\|Behave\.Assets\.\|Behave\.EPriority\|Behave\.EState\|Behave\.Init' Assets/Scripts/ | grep -oP 'Behave\.\w+(?:\.\w+)?' | sort | uniq -c | sort -rn`
    - `grep -rn 'FMODAudioSource\.' Assets/ | grep -oP 'FMODAudioSource\.\w+\|audioSrc\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'FMODAudioListener\.' Assets/ | grep -oP 'FMODAudioListener\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'FMOD_StudioSystem\.' Assets/ | grep -oP 'FMOD_StudioSystem\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'FMODAsset\.' Assets/ | grep -oP 'FMODAsset\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'FMOD\.Studio\.\|FMOD\.RESULT\|FMOD\.GUID' Assets/ | head -20`
    - Read Assets/PeAudio/PeFmodEditor/FMODAudioSourceRTE.cs for FMOD type usage
  </read_first>
  <action>
    Create Assets/Stubs/Behave/ and Assets/Stubs/FMOD/ directories.

    **Behave stubs** (in `namespace Behave.Runtime`):

    `BehaveRuntime.cs` -- Single file containing all proprietary Behave types:

    - `enum BehaveResult { Success, Failure, Running }` (2110 total references -- THE most used stub type)
    - `interface IAgent` with methods:
      - `void Reset(Tree sender)`
      - `int SelectTopPriority(Tree sender, params int[] IDs)`
      - `BehaveResult Tick(Tree sender)`
    - `class Tree` with:
      - `float Frequency` property (used in BTAgent for tick interval)
      - `Type LibraryActions` property (used in BTAgent for enum lookup)
      - `int LastTickedAction` property (used in BTAgent for action tracking)
      - `BehaveResult Tick(IAgent agent, object data)` method (core tick)
      - `void Reset()` method (parameterless overload)
      - `void Reset(IAgent agent, Tree tree, object data)` method (3-param overload used in BTAgent)
      - `BehaveResult Result(bool value)` static utility

    Also provide in `namespace Behave`:
    - `enum EPriority { p_1, p_2, p_3, p_4, p_5 }` (referenced as Behave.EPriority.p_5 etc.)
    - `enum EState { Preparing, Pause, Finished }` (referenced as Behave.EState.Preparing etc.)

    Also provide in `namespace Behave.Assets`:
    - `class Tree` (asset representation, different from Runtime.Tree)
    - `class Reference` (asset reference)
    - `class Decorator` (asset decorator)
    - `class Action` (asset action)
    - `class Component` (asset component)

    IMPORTANT: The in-house code (BTAgent, BTResolver, BTLauncher, BTAction, BehaveAction, BehaveAttribute, Reflecter, BTCoroutine, BTExtension, BTInterface) is already in Assets/Scripts/BehaviorTree/Scripts/. Do NOT duplicate those. Only stub the types that come from the proprietary Behave DLL.

    **FMOD stubs**:

    1. `FMODAudioSource.cs` -- `public class FMODAudioSource : MonoBehaviour` (global namespace, matching FMOD Unity integration convention)
       - Properties/fields referenced by game code. Check grep results for exact members.

    2. `FMODAudioListener.cs` -- `public class FMODAudioListener : MonoBehaviour`
       - `static listener` property (referenced by FMODAudioSourceRTE.cs)

    3. `FMODAsset.cs` -- `public class FMODAsset : ScriptableObject`
       - Basic asset reference type

    4. `FMOD_StudioSystem.cs` -- `public class FMOD_StudioSystem : MonoBehaviour`
       - Singleton pattern, no-op

    5. `FMODStudio.cs` -- Types in FMOD and FMOD.Studio namespaces:
       - `namespace FMOD`:
         - `enum RESULT { OK, ERR_INVALID_HANDLE, ERR_CHANNEL_STOLEN, ... }` (include values referenced)
         - `struct GUID` (empty)
         - `class Sound` (empty)
         - `class Channel` (empty)
       - `namespace FMOD.Studio`:
         - `class EventDescription` (has `getPath()`, `createInstance()`)
         - `class EventInstance` (has `start()`, `stop()`, `release()`, `setParameterValue()`, `set3DAttributes()`)
         - `class Bank` (has `getPath()`, `loadSampleData()`)
         - `class Bus` (has `setVolume()`, `getVolume()`)
         - `class VCA` (has `setVolume()`, `getVolume()`)
         - `struct PARAMETER_DESCRIPTION` (has `name`, `minimum`, `maximum`, `defaultvalue`)
  </action>
  <verify>
    <automated>ls Assets/Stubs/Behave/*.cs Assets/Stubs/FMOD/*.cs 2>/dev/null | wc -l</automated>
    Expected: 6 files (1 Behave + 5 FMOD)
  </verify>
  <acceptance_criteria>
    - `grep -l "enum BehaveResult" Assets/Stubs/Behave/BehaveRuntime.cs` returns the file
    - `grep "Success.*Failure.*Running" Assets/Stubs/Behave/BehaveRuntime.cs` confirms all 3 enum values
    - `grep -l "interface IAgent" Assets/Stubs/Behave/BehaveRuntime.cs` returns the file
    - `grep -l "class Tree" Assets/Stubs/Behave/BehaveRuntime.cs` returns the file
    - `grep -l "enum EPriority" Assets/Stubs/Behave/BehaveRuntime.cs` returns the file
    - `grep -l "public class FMODAudioSource" Assets/Stubs/FMOD/FMODAudioSource.cs` returns the file
    - `grep -l "public class FMODAudioListener" Assets/Stubs/FMOD/FMODAudioListener.cs` returns the file
    - `grep -l "namespace FMOD.Studio" Assets/Stubs/FMOD/FMODStudio.cs` returns the file
    - `grep -l "class EventDescription" Assets/Stubs/FMOD/FMODStudio.cs` returns the file
  </acceptance_criteria>
  <done>Behave runtime stubs provide BehaveResult (3 values), IAgent interface, Tree class, EPriority/EState enums, and Behave.Assets types. FMOD stubs provide FMODAudioSource, FMODAudioListener, FMOD_StudioSystem, FMODAsset, and FMOD.Studio namespace types. All gameplay middleware type references resolve.</done>
</task>

</tasks>

<verification>
1. FinalIK stubs: `ls Assets/Stubs/FinalIK/*.cs | wc -l` -- expect 6
2. A* stubs: `ls Assets/Stubs/AstarPathfinding/*.cs | wc -l` -- expect 7
3. Behave stubs: `grep -c "enum BehaveResult\|interface IAgent\|class Tree" Assets/Stubs/Behave/BehaveRuntime.cs` -- expect 3
4. FMOD stubs: `ls Assets/Stubs/FMOD/*.cs | wc -l` -- expect 5
5. Correct namespaces: `grep "namespace RootMotion.FinalIK" Assets/Stubs/FinalIK/*.cs | wc -l` + `grep "namespace Pathfinding" Assets/Stubs/AstarPathfinding/*.cs | wc -l` + `grep "namespace Behave" Assets/Stubs/Behave/*.cs | wc -l` -- all positive
</verification>

<success_criteria>
- Every FinalIK type in `grep -rn 'RootMotion.FinalIK' Assets/Scripts/` has a stub
- Every Pathfinding type in `grep -rn 'Pathfinding\.' Assets/Scripts/` has a stub
- BehaveResult enum has Success, Failure, Running values
- IAgent interface has Reset, SelectTopPriority, Tick methods
- Tree class has Frequency, Tick, Reset methods
- FMODAudioSource and FMODAudioListener exist as MonoBehaviours
</success_criteria>

<output>
After completion, create `.planning/phases/02-proprietary-plugin-stubs/02-03-SUMMARY.md`
</output>
