---
phase: 02-proprietary-plugin-stubs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Stubs/uLink/MonoBehaviour.cs
  - Assets/Stubs/uLink/BitStream.cs
  - Assets/Stubs/uLink/NetworkMessageInfo.cs
  - Assets/Stubs/uLink/BitStreamCodec.cs
  - Assets/Stubs/uLink/Network.cs
  - Assets/Stubs/uLink/NetworkView.cs
  - Assets/Stubs/uLink/NetworkViewID.cs
  - Assets/Stubs/uLink/MasterServer.cs
  - Assets/Stubs/uLink/HostData.cs
  - Assets/Stubs/uLink/RPCMode.cs
  - Assets/Stubs/uLink/NetworkTypes.cs
  - Assets/Stubs/uLink/RegisterPrefabs.cs
  - Assets/Stubs/uLobby/Lobby.cs
  - Assets/Stubs/uLobby/ServerInfo.cs
  - Assets/Stubs/uLobby/ServerRegistry.cs
  - Assets/Stubs/uLobby/LobbyConnectionError.cs
  - Assets/Stubs/Steamworks/SteamAPI.cs
  - Assets/Stubs/Steamworks/SteamManager.cs
  - Assets/Stubs/Steamworks/SteamTypes.cs
  - Assets/Stubs/Steamworks/SteamFriends.cs
  - Assets/Stubs/Steamworks/SteamUserStats.cs
  - Assets/Stubs/Steamworks/SteamUser.cs
  - Assets/Stubs/Steamworks/SteamUtils.cs
  - Assets/Stubs/Steamworks/SteamUGC.cs
  - Assets/Stubs/Steamworks/SteamMatchmaking.cs
  - Assets/Stubs/Steamworks/SteamClient.cs
  - Assets/Stubs/Steamworks/SteamApps.cs
  - Assets/Stubs/Steamworks/Callback.cs
  - Assets/Stubs/Steamworks/CallResult.cs
autonomous: true
requirements:
  - ENV-02

must_haves:
  truths:
    - "All files using uLink.MonoBehaviour, uLink.BitStream, uLink.Network compile without errors"
    - "All files using uLobby.Lobby, uLobby.ServerInfo compile without errors"
    - "All files using Steamworks.NET types compile without errors"
    - "uLink.MonoBehaviour extends UnityEngine.MonoBehaviour and returns isMine=true, isServer=true for single-player"
  artifacts:
    - path: "Assets/Stubs/uLink/MonoBehaviour.cs"
      provides: "uLink.MonoBehaviour base class for all networked objects"
      contains: "namespace uLink"
    - path: "Assets/Stubs/uLink/BitStream.cs"
      provides: "uLink.BitStream serialization stub (681 references)"
      contains: "public class BitStream"
    - path: "Assets/Stubs/uLink/Network.cs"
      provides: "uLink.Network static class for connection management"
      contains: "public static class Network"
    - path: "Assets/Stubs/uLobby/Lobby.cs"
      provides: "uLobby.Lobby stub"
      contains: "namespace uLobby"
    - path: "Assets/Stubs/Steamworks/SteamAPI.cs"
      provides: "Steamworks SteamAPI initialization stub"
      contains: "public static class SteamAPI"
  key_links:
    - from: "Assets/Scripts/GameNetwork/"
      to: "Assets/Stubs/uLink/"
      via: "namespace uLink type resolution"
      pattern: "uLink\\.MonoBehaviour|uLink\\.BitStream|uLink\\.Network"
    - from: "Assets/Scripts/Steamwork.NET/"
      to: "Assets/Stubs/Steamworks/"
      via: "Steamworks namespace resolution"
      pattern: "Steamworks\\."
---

<objective>
Create stub source files for uLink, uLobby, and Steamworks.NET -- the networking and platform integration plugins. uLink is referenced heavily (681 BitStream uses, 592 NetworkMessageInfo uses) but all networking is no-op for single-player. Steamworks is referenced in ~28 files but unnecessary for standalone builds.

Purpose: Eliminate all CS0246 missing-type errors for networking and platform types. Single-player stubs return sane defaults (isMine=true, isServer=true) so existing code paths select the correct local-player branches.

Output: Assets/Stubs/uLink/, Assets/Stubs/uLobby/, Assets/Stubs/Steamworks/ directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL CONSTRAINTS (from research/PITFALLS.md):
- uLink.MonoBehaviour MUST extend UnityEngine.MonoBehaviour. Many game classes inherit from it.
- Single-player stubs must return isMine=true, isServer=true so local-player code paths execute.
- uLink.BitStream is the most heavily used type (681 references). It needs Read/Write methods for all primitive types.
- Steamworks.NET uses a Steamworks namespace with CSteamID struct, various static classes, and callback patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create uLink and uLobby networking stubs</name>
  <files>
    Assets/Stubs/uLink/MonoBehaviour.cs,
    Assets/Stubs/uLink/BitStream.cs,
    Assets/Stubs/uLink/NetworkMessageInfo.cs,
    Assets/Stubs/uLink/BitStreamCodec.cs,
    Assets/Stubs/uLink/Network.cs,
    Assets/Stubs/uLink/NetworkView.cs,
    Assets/Stubs/uLink/NetworkViewID.cs,
    Assets/Stubs/uLink/MasterServer.cs,
    Assets/Stubs/uLink/HostData.cs,
    Assets/Stubs/uLink/RPCMode.cs,
    Assets/Stubs/uLink/NetworkTypes.cs,
    Assets/Stubs/uLink/RegisterPrefabs.cs,
    Assets/Stubs/uLobby/Lobby.cs,
    Assets/Stubs/uLobby/ServerInfo.cs,
    Assets/Stubs/uLobby/ServerRegistry.cs,
    Assets/Stubs/uLobby/LobbyConnectionError.cs
  </files>
  <read_first>
    Discover exact API surfaces used:
    - `grep -rn 'uLink\.BitStream' Assets/Scripts/ | grep -oP '\.(?:Read|Write|TryRead)\w+' | sort | uniq -c | sort -rn` (find all Read/Write methods on BitStream)
    - `grep -rn '\.networkView\b\|\.isMine\b\|\.isServer\b\|\.isClient\b\|\.isProxy\b\|\.isOwner\b' Assets/Scripts/ | head -30` (MonoBehaviour properties)
    - `grep -rn 'uLink\.Network\.' Assets/Scripts/ | grep -oP 'Network\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'uLink\.NetworkView\.' Assets/Scripts/ | grep -oP 'NetworkView\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'uLink\.NetworkViewID\.' Assets/Scripts/ | grep -oP 'NetworkViewID\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'NetworkMessageInfo\.' Assets/Scripts/ | grep -oP 'NetworkMessageInfo\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'BitStreamCodec\.' Assets/Scripts/ | grep -oP 'BitStreamCodec\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'uLink\.MasterServer\.' Assets/Scripts/ | grep -oP 'MasterServer\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'uLink\.HostData' Assets/Scripts/ | head -10`
    - `grep -rn 'RPCMode\.' Assets/Scripts/ | grep -oP 'RPCMode\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'uLink\.NetworkPeer\|uLink\.NetworkStatus\|uLink\.NetworkDisconnection\|uLink\.NetworkConnectionError\|uLink\.NetworkP2P\|uLink\.NetworkLog\|uLink\.PublicKey\|uLink\.NetworkBufferedRPC\|uLink\.MasterServerEvent\|uLink\.RegisterPrefabs' Assets/Scripts/ | head -20`
    - `grep -rn 'uLobby\.' Assets/Scripts/ | head -20` (all uLobby usage)
    Also examine key networking files:
    - Read Assets/Scripts/GameNetwork/NetworkInterface.cs (first 100 lines)
    - Read Assets/Scripts/GameNetwork/NetworkManager.cs (first 100 lines)
  </read_first>
  <action>
    Create Assets/Stubs/uLink/ and Assets/Stubs/uLobby/ directories.

    **uLink stubs** (all in `namespace uLink`):

    1. `MonoBehaviour.cs` -- class `MonoBehaviour : UnityEngine.MonoBehaviour`
       - Properties: `networkView` (returns a stub NetworkView), `isMine` (returns true), `isServer` (returns true), `isClient` (returns false), `isOwner` (returns true), `isProxy` (returns false)
       - Methods: `uLink_OnSerializeNetworkView(BitStream, NetworkMessageInfo)` virtual no-op, RPC method stubs
       - This is THE critical stub -- many game classes inherit from this

    2. `BitStream.cs` -- class `BitStream`
       - Must have Read/Write methods for ALL types the game code uses: `Read<T>()`, `Write<T>()`, `ReadBoolean()`, `ReadInt32()`, `ReadSingle()`, `ReadString()`, `ReadVector3()`, `ReadQuaternion()`, `ReadByte()`, `ReadBytes()`, `ReadChar()`, `ReadInt16()`, `ReadUInt16()`, `ReadUInt32()`, `ReadInt64()`, `ReadColor()`, plus matching Write methods
       - `TryRead<T>()` variants
       - Properties: `isReading`, `isWriting`, `bitsRead`, `bitsWritten`
       - All Read methods return default(T). All Write methods are no-ops.

    3. `NetworkMessageInfo.cs` -- struct `NetworkMessageInfo`
       - Fields: `sender` (NetworkPlayer), `timestamp` (double), `networkView` (NetworkView)

    4. `BitStreamCodec.cs` -- static class for registering custom serializers
       - `Add<T>(...)` static methods as no-ops

    5. `Network.cs` -- static class `Network`
       - Properties: `isServer`, `isClient`, `peerType`, `status`, `player`, `connections`
       - Methods: `Connect()`, `Disconnect()`, `InitializeServer()`, `CloseConnection()`, `Destroy()`, `Instantiate()`, `RPC()` -- all no-ops
       - For single-player: `isServer = true`, `isClient = false`

    6. `NetworkView.cs` -- class `NetworkView : UnityEngine.MonoBehaviour`
       - Properties: `viewID` (NetworkViewID), `isMine` (true), `owner` (NetworkPlayer)
       - Methods: `RPC()` (no-op), `UnreliableRPC()` (no-op)

    7. `NetworkViewID.cs` -- struct `NetworkViewID`
       - `Unassigned` static field, equality operators

    8. `RPCMode.cs` -- enum with Server, Others, All, OthersBuffered, AllBuffered members

    9. `NetworkTypes.cs` -- consolidate small types:
       - `NetworkPlayer` struct (has id, isServer, isMine)
       - `NetworkPeer` enum
       - `NetworkPeerType` enum (Server, Client, Disconnected, Connecting)
       - `NetworkStatus` enum
       - `NetworkDisconnection` enum
       - `NetworkConnectionError` enum
       - `NetworkLogFlags` enum
       - `NetworkLogLevel` enum
       - `NetworkP2P` static class (no-op)
       - `NetworkP2PMessageInfo` struct
       - `NetworkUtility` static class
       - `NetworkLog` static class
       - `NetworkLogUtility` static class
       - `NetworkBufferedRPC` class
       - `PublicKey` class
       - `MasterServerEvent` enum

    10. `MasterServer.cs` -- static class with no-op methods

    11. `HostData.cs` -- class with server info fields

    12. `RegisterPrefabs.cs` -- class RegisterPrefabs : MonoBehaviour (no-op)

    **uLobby stubs** (all in `namespace uLobby`):

    1. `Lobby.cs` -- static class `Lobby` with Connect/Disconnect/no-op methods
    2. `ServerInfo.cs` -- class with server info fields
    3. `ServerRegistry.cs` -- class with no-op methods
    4. `LobbyConnectionError.cs` -- enum

    All uLobby methods are no-ops. The lobby system is entirely unnecessary for single-player.
  </action>
  <verify>
    <automated>grep -rl "namespace uLink" Assets/Stubs/uLink/ | wc -l && grep -rl "namespace uLobby" Assets/Stubs/uLobby/ | wc -l</automated>
    Expected: 12 uLink files + 4 uLobby files
  </verify>
  <acceptance_criteria>
    - `grep -l "class MonoBehaviour : UnityEngine.MonoBehaviour" Assets/Stubs/uLink/MonoBehaviour.cs` returns the file
    - `grep -l "public class BitStream" Assets/Stubs/uLink/BitStream.cs` returns the file
    - `grep "isMine" Assets/Stubs/uLink/MonoBehaviour.cs` shows isMine returning true
    - `grep "isServer" Assets/Stubs/uLink/MonoBehaviour.cs` shows isServer returning true
    - `grep -l "ReadInt32\|ReadSingle\|ReadString\|ReadVector3" Assets/Stubs/uLink/BitStream.cs` confirms Read methods exist
    - `grep -l "namespace uLobby" Assets/Stubs/uLobby/Lobby.cs` returns the file
    - `grep -l "enum RPCMode" Assets/Stubs/uLink/RPCMode.cs` returns the file
  </acceptance_criteria>
  <done>uLink stubs define all networking types with single-player defaults (isMine=true, isServer=true). uLobby stubs define all lobby types as no-ops. All files referencing uLink and uLobby types will resolve their type references.</done>
</task>

<task type="auto">
  <name>Task 2: Create Steamworks.NET stubs</name>
  <files>
    Assets/Stubs/Steamworks/SteamAPI.cs,
    Assets/Stubs/Steamworks/SteamManager.cs,
    Assets/Stubs/Steamworks/SteamTypes.cs,
    Assets/Stubs/Steamworks/SteamFriends.cs,
    Assets/Stubs/Steamworks/SteamUserStats.cs,
    Assets/Stubs/Steamworks/SteamUser.cs,
    Assets/Stubs/Steamworks/SteamUtils.cs,
    Assets/Stubs/Steamworks/SteamUGC.cs,
    Assets/Stubs/Steamworks/SteamMatchmaking.cs,
    Assets/Stubs/Steamworks/SteamClient.cs,
    Assets/Stubs/Steamworks/SteamApps.cs,
    Assets/Stubs/Steamworks/Callback.cs,
    Assets/Stubs/Steamworks/CallResult.cs
  </files>
  <read_first>
    Discover exact Steamworks API surfaces:
    - `grep -rn 'Steamworks\.' Assets/Scripts/ | grep -oP 'Steamworks\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'CSteamID\.' Assets/Scripts/ | grep -oP 'CSteamID\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamFriends\.' Assets/Scripts/ | grep -oP 'SteamFriends\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamUserStats\.' Assets/Scripts/ | grep -oP 'SteamUserStats\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamUser\.' Assets/Scripts/ | grep -oP 'SteamUser\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamUGC\.' Assets/Scripts/ | grep -oP 'SteamUGC\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamUtils\.' Assets/Scripts/ | grep -oP 'SteamUtils\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SteamApps\.' Assets/Scripts/ | grep -oP 'SteamApps\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'CallResult<\|Callback<' Assets/Scripts/ | head -20`
    - `grep -rn 'SteamManager\.' Assets/Scripts/ | grep -oP 'SteamManager\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'EResult\.' Assets/Scripts/ | grep -oP 'EResult\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'PublishedFileId_t\|UGCHandle_t\|AppId_t\|SteamAPICall_t\|AccountID_t' Assets/Scripts/ | head -20`
    - Read Assets/Scripts/Steamwork.NET/SteamManager.cs (first 80 lines)
  </read_first>
  <action>
    Create Assets/Stubs/Steamworks/ directory. All types in `namespace Steamworks` (matching Steamworks.NET convention).

    1. `SteamTypes.cs` -- value types and enums used across the API:
       - `CSteamID` struct (has m_SteamID ulong field, IsValid(), GetAccountID(), static op_Implicit from/to ulong, equality operators)
       - `AppId_t` struct (has m_AppId uint)
       - `PublishedFileId_t` struct (has m_PublishedFileId ulong)
       - `UGCHandle_t` struct (has m_UGCHandle ulong)
       - `SteamAPICall_t` struct (has m_SteamAPICall ulong)
       - `AccountID_t` struct (has m_AccountID uint)
       - `EResult` enum (OK, Fail, NoConnection, etc. -- include all values referenced by game code)
       - `EPersonaState` enum
       - `EFriendRelationship` enum
       - `EUGCQuery` enum
       - `EUGCMatchingUGCType` enum
       - `EUserUGCList` enum
       - `EUserUGCListSortOrder` enum
       - `EItemStatistic` enum
       - `EItemUpdateStatus` enum
       - `UGCQueryHandle_t` struct
       - `UGCUpdateHandle_t` struct
       - `SteamUGCDetails_t` struct (with m_nPublishedFileId, m_rgchTitle, m_rgchDescription, m_eResult fields)
       - `SteamUGCQueryCompleted_t` struct (with m_handle, m_eResult, m_unNumResultsReturned, m_unTotalMatchingResults)
       - `PersonaStateChange_t` struct
       - `GameOverlayActivated_t` struct

    2. `SteamAPI.cs` -- `public static class SteamAPI`
       - Init(), Shutdown(), RunCallbacks(), IsSteamRunning() -- all no-ops, Init returns true
       - RestartAppIfNecessary(AppId_t) returns false

    3. `SteamManager.cs` -- NOTE: The game already has its own SteamManager.cs in Assets/Scripts/Steamwork.NET/. The stub should provide any Steamworks types that SteamManager.cs references. Do NOT create a duplicate SteamManager class -- only provide the Steamworks API types it calls.

    4. `SteamFriends.cs` -- static class with GetPersonaName(), GetFriendCount(), GetFriendByIndex(), GetFriendPersonaName(), etc. All return defaults.

    5. `SteamUserStats.cs` -- static class with RequestCurrentStats(), GetStat(), SetStat(), StoreStats(), SetAchievement(), GetAchievement() etc. Return defaults.

    6. `SteamUser.cs` -- static class with GetSteamID() returns new CSteamID(), BLoggedOn() returns false

    7. `SteamUtils.cs` -- static class with GetAppID(), GetIPCountry(), IsOverlayEnabled() etc.

    8. `SteamUGC.cs` -- static class with CreateQueryUserUGCRequest(), SendQueryUGCRequest(), GetQueryUGCResult(), ReleaseQueryUGCRequest(), CreateItem(), StartItemUpdate(), SetItemTitle(), SubmitItemUpdate(), GetItemDownloadInfo(), GetItemState(), DownloadItem(), etc. All return defaults.

    9. `SteamMatchmaking.cs` -- static class with no-op methods

    10. `SteamClient.cs` -- static class with no-op methods

    11. `SteamApps.cs` -- static class with GetCurrentGameLanguage(), BIsSubscribedApp() etc.

    12. `Callback.cs` -- `public class Callback<T>` with Create() static factory, no-op

    13. `CallResult.cs` -- `public class CallResult<T>` with Set() method, no-op

    SteamAPI.Init() should return true but log a warning that Steam integration is stubbed.
  </action>
  <verify>
    <automated>grep -rl "namespace Steamworks" Assets/Stubs/Steamworks/ | wc -l</automated>
    Expected: 13 files
  </verify>
  <acceptance_criteria>
    - `grep -l "public static class SteamAPI" Assets/Stubs/Steamworks/SteamAPI.cs` returns the file
    - `grep -l "public struct CSteamID" Assets/Stubs/Steamworks/SteamTypes.cs` returns the file
    - `grep -l "public struct PublishedFileId_t" Assets/Stubs/Steamworks/SteamTypes.cs` returns the file
    - `grep -l "public class CallResult" Assets/Stubs/Steamworks/CallResult.cs` returns the file
    - `grep -l "public class Callback" Assets/Stubs/Steamworks/Callback.cs` returns the file
    - `grep -l "public static class SteamFriends" Assets/Stubs/Steamworks/SteamFriends.cs` returns the file
    - `grep -l "public enum EResult" Assets/Stubs/Steamworks/SteamTypes.cs` returns the file
  </acceptance_criteria>
  <done>All Steamworks.NET types used by the ~28 game files are defined as stubs. SteamAPI.Init() returns true. All Steam calls are no-ops. Combined with Task 1, networking and platform integration are fully stubbed.</done>
</task>

</tasks>

<verification>
1. uLink stubs: `ls Assets/Stubs/uLink/*.cs | wc -l` -- expect 12
2. uLobby stubs: `ls Assets/Stubs/uLobby/*.cs | wc -l` -- expect 4
3. Steamworks stubs: `ls Assets/Stubs/Steamworks/*.cs | wc -l` -- expect 13
4. Single-player defaults: `grep "true" Assets/Stubs/uLink/MonoBehaviour.cs | grep -c "isMine\|isServer"` -- expect 2
5. BitStream completeness: `grep -c "public.*Read\|public.*Write" Assets/Stubs/uLink/BitStream.cs` -- expect 30+
</verification>

<success_criteria>
- Every uLink type referenced via `grep -rn 'uLink\.' Assets/Scripts/` has a corresponding stub
- Every uLobby type referenced via `grep -rn 'uLobby\.' Assets/Scripts/` has a corresponding stub
- Every Steamworks type referenced via `grep -rn 'Steamworks\.' Assets/Scripts/` has a corresponding stub
- uLink.MonoBehaviour returns isMine=true, isServer=true for single-player operation
</success_criteria>

<output>
After completion, create `.planning/phases/02-proprietary-plugin-stubs/02-02-SUMMARY.md`
</output>
