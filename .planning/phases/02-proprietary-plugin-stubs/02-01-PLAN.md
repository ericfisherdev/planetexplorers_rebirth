---
phase: 02-proprietary-plugin-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Stubs/NGUI/UILabel.cs
  - Assets/Stubs/NGUI/UISprite.cs
  - Assets/Stubs/NGUI/UIPanel.cs
  - Assets/Stubs/NGUI/UIButton.cs
  - Assets/Stubs/NGUI/UIWidget.cs
  - Assets/Stubs/NGUI/UITexture.cs
  - Assets/Stubs/NGUI/UIGrid.cs
  - Assets/Stubs/NGUI/UITable.cs
  - Assets/Stubs/NGUI/UIAnchor.cs
  - Assets/Stubs/NGUI/UIRoot.cs
  - Assets/Stubs/NGUI/UICamera.cs
  - Assets/Stubs/NGUI/UISlider.cs
  - Assets/Stubs/NGUI/UICheckbox.cs
  - Assets/Stubs/NGUI/UIScrollBar.cs
  - Assets/Stubs/NGUI/UIInput.cs
  - Assets/Stubs/NGUI/UIPopupList.cs
  - Assets/Stubs/NGUI/UIEventListener.cs
  - Assets/Stubs/NGUI/UITweener.cs
  - Assets/Stubs/NGUI/UIFont.cs
  - Assets/Stubs/NGUI/UIAtlas.cs
  - Assets/Stubs/NGUI/UISlicedSprite.cs
  - Assets/Stubs/NGUI/UIDrawCall.cs
  - Assets/Stubs/NGUI/UIRect.cs
  - Assets/Stubs/NGUI/UIBasicSprite.cs
  - Assets/Stubs/NGUI/TweenPosition.cs
  - Assets/Stubs/NGUI/TweenRotation.cs
  - Assets/Stubs/NGUI/TweenScale.cs
  - Assets/Stubs/NGUI/TweenAlpha.cs
  - Assets/Stubs/NGUI/TweenColor.cs
  - Assets/Stubs/NGUI/EventDelegate.cs
  - Assets/Stubs/NGUI/SpringPanel.cs
  - Assets/Stubs/NGUI/NGUITools.cs
  - Assets/Stubs/NGUI/UIDragScrollView.cs
  - Assets/Stubs/NGUI/UIScrollView.cs
  - Assets/Stubs/NGUI/UIProgressBar.cs
  - Assets/Stubs/NGUI/UIToggle.cs
  - Assets/Stubs/NGUI/UISpriteAnimation.cs
  - Assets/Stubs/NGUI/UIWrapContent.cs
  - Assets/Stubs/NGUI/UIPlayTween.cs
autonomous: true
requirements:
  - ENV-02

must_haves:
  truths:
    - "All 417+ files referencing NGUI types compile without CS0246 missing-type errors"
    - "NGUI types are in the global namespace (no 'using NGUI;' needed)"
    - "UILabel, UISprite, UIPanel, UIButton and all other NGUI components exist as MonoBehaviour stubs"
  artifacts:
    - path: "Assets/Stubs/NGUI/UILabel.cs"
      provides: "UILabel stub with text, fontSize, color, overflowMethod properties"
      contains: "public class UILabel"
    - path: "Assets/Stubs/NGUI/UISprite.cs"
      provides: "UISprite stub with spriteName, atlas, fillAmount properties"
      contains: "public class UISprite"
    - path: "Assets/Stubs/NGUI/UIPanel.cs"
      provides: "UIPanel stub with clipping, alpha, depth properties"
      contains: "public class UIPanel"
    - path: "Assets/Stubs/NGUI/NGUITools.cs"
      provides: "NGUITools static utility methods"
      contains: "public static class NGUITools"
    - path: "Assets/Stubs/NGUI/EventDelegate.cs"
      provides: "EventDelegate class for NGUI event system"
      contains: "public class EventDelegate"
  key_links:
    - from: "Assets/Scripts/Chenzhi/NewUI/"
      to: "Assets/Stubs/NGUI/"
      via: "global namespace type resolution"
      pattern: "UILabel|UISprite|UIPanel|UIButton"
---

<objective>
Create stub C# source files for all NGUI types referenced across the 417+ files in the codebase. Every NGUI type (UILabel, UISprite, UIPanel, UIButton, UIWidget, UITexture, UIGrid, UITable, UICheckbox, UISlider, UIScrollBar, UIInput, UIPopupList, UIEventListener, UITweener, UIFont, UIAtlas, UICamera, UIAnchor, UIRoot, UISlicedSprite, UIDrawCall, UIRect, TweenPosition/Rotation/Scale/Alpha/Color, EventDelegate, SpringPanel, NGUITools, etc.) must be defined as a no-op MonoBehaviour or static class in the global namespace.

Purpose: NGUI is the single largest proprietary dependency (417+ files). These stubs eliminate all CS0246 missing-type errors for NGUI types, enabling the codebase to compile. Stubs will be replaced with functional uGUI equivalents in Phase 8.

Output: Assets/Stubs/NGUI/ directory containing all stub source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL CONSTRAINTS (from research/PITFALLS.md):
- NGUI types MUST be in the GLOBAL namespace (no `namespace NGUI { }` wrapper). Game code uses `UILabel` directly, never `NGUI.UILabel`.
- Stubs are C# source files placed in Assets/Stubs/NGUI/, NOT pre-compiled DLLs. This lets Unity handle GUID assignment naturally and avoids scene file GUID mismatch issues.
- All stub MonoBehaviours must extend UnityEngine.MonoBehaviour.
- Properties and fields referenced by game code must exist with correct types and default values.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NGUI core widget and container stubs</name>
  <files>
    Assets/Stubs/NGUI/UIWidget.cs,
    Assets/Stubs/NGUI/UIRect.cs,
    Assets/Stubs/NGUI/UIBasicSprite.cs,
    Assets/Stubs/NGUI/UILabel.cs,
    Assets/Stubs/NGUI/UISprite.cs,
    Assets/Stubs/NGUI/UISlicedSprite.cs,
    Assets/Stubs/NGUI/UITexture.cs,
    Assets/Stubs/NGUI/UIPanel.cs,
    Assets/Stubs/NGUI/UICamera.cs,
    Assets/Stubs/NGUI/UIAnchor.cs,
    Assets/Stubs/NGUI/UIRoot.cs,
    Assets/Stubs/NGUI/UIDrawCall.cs,
    Assets/Stubs/NGUI/UIFont.cs,
    Assets/Stubs/NGUI/UIAtlas.cs,
    Assets/Stubs/NGUI/NGUITools.cs,
    Assets/Stubs/NGUI/EventDelegate.cs
  </files>
  <read_first>
    Run these grep commands to discover the exact properties/methods/fields used for each type:
    - `grep -rn 'UILabel\.' Assets/Scripts/ | grep -oP 'UILabel\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'UISprite\.' Assets/Scripts/ | grep -oP 'UISprite\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'UIPanel\.' Assets/Scripts/ | grep -oP 'UIPanel\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'UIWidget\.' Assets/Scripts/ | grep -oP 'UIWidget\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'UICamera\.' Assets/Scripts/ | grep -oP 'UICamera\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'NGUITools\.' Assets/Scripts/ | grep -oP 'NGUITools\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'EventDelegate\.' Assets/Scripts/ | grep -oP 'EventDelegate\.\w+' | sort | uniq -c | sort -rn | head -20`
    - `grep -rn 'UIAtlas\.' Assets/Scripts/ | grep -oP 'UIAtlas\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIFont\.' Assets/Scripts/ | grep -oP 'UIFont\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIDrawCall\.' Assets/Scripts/ | grep -oP 'UIDrawCall\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIRect\.' Assets/Scripts/ | grep -oP 'UIRect\.\w+' | sort | uniq -c | sort -rn`
    Also check for enum types used with NGUI:
    - `grep -rn 'UILabel\.Overflow\|UILabel\.Effect\|UILabel\.Crispness' Assets/Scripts/ | head -10`
    - `grep -rn 'UIWidget\.Pivot\|UIWidget\.AspectRatioSource' Assets/Scripts/ | head -10`
    - `grep -rn 'UIBasicSprite\.Type\|UIBasicSprite\.FillDirection' Assets/Scripts/ | head -10`
  </read_first>
  <action>
    Create Assets/Stubs/NGUI/ directory. For each NGUI type below, create a .cs file in the GLOBAL namespace (no namespace declaration) with all properties, fields, methods, enums, and delegates that the game code references. Every type that is a component should extend MonoBehaviour. Types should form correct inheritance chains where game code expects it:

    Inheritance chain (matching real NGUI):
    - UIRect : MonoBehaviour
    - UIWidget : UIRect (has Pivot enum, color, width, height, depth, alpha, pivot, onChange)
    - UIBasicSprite : UIWidget (has Type enum, FillDirection enum, fillAmount, fillDirection, invert, type)
    - UISprite : UIBasicSprite (has spriteName, atlas, MakePixelPerfect())
    - UISlicedSprite : UISprite (empty subclass for backward compat)
    - UILabel : UIWidget (has text, fontSize, font, overflowMethod, Overflow enum, Effect enum, effectColor, effectDistance, spacingX, spacingY, maxLineCount, supportEncoding, symbolStyle, printedSize, processedText, alignment)
    - UITexture : UIWidget (has mainTexture, material, shader)
    - UIPanel : UIRect (has alpha, depth, clipping, clipOffset, baseClipRegion, widgetsAreStatic, Find(), GetComponentsInChildren)
    - UICamera : MonoBehaviour (has static current, static currentCamera, static mainCamera, static currentTouch, static hoveredObject, static isOverUI, eventReceiverMask, static Notify(), onClick, onHover, onPress, onSelect, onScroll, onDrag)
    - UIAnchor : MonoBehaviour (has container, side, relativeOffset, pixelOffset, uiCamera)
    - UIRoot : MonoBehaviour (has static list, scalingStyle, manualHeight, minimumHeight, maximumHeight)
    - UIFont : MonoBehaviour (has bmFont, dynamicFont, dynamicFontSize, atlas, spriteName, uvRect)
    - UIAtlas : MonoBehaviour (has spriteList, spriteMaterial, GetSprite())
    - UIDrawCall : MonoBehaviour (empty stub)

    Standalone classes:
    - NGUITools : public static class (AddChild, SetActive, Destroy, FindInParents, GetActive, SetLayer, MakePixelPerfect, AddWidgetCollider, GetRoot, GetTypeName, GetHierarchy)
    - EventDelegate : public class (has List type, Add(), Remove(), Set(), Execute(), IsValid, static Execute(List<EventDelegate>), static Add(List<EventDelegate>, EventDelegate.Callback), Callback delegate)

    Every property getter returns a sensible default (null for objects, 0 for numbers, "" for strings, false for bools, Color.white for colors). Every method body is empty or returns default. Every enum referenced in game code must be defined with correct member names.
  </action>
  <verify>
    <automated>grep -rl "public class UILabel\|public class UISprite\|public class UIPanel\|public class UICamera\|public static class NGUITools\|public class EventDelegate" Assets/Stubs/NGUI/ | wc -l</automated>
    Expected: 6 (one file each for UILabel, UISprite, UIPanel, UICamera, NGUITools, EventDelegate)
  </verify>
  <acceptance_criteria>
    - `grep -l "public class UILabel" Assets/Stubs/NGUI/UILabel.cs` returns the file
    - `grep -l "public class UISprite" Assets/Stubs/NGUI/UISprite.cs` returns the file
    - `grep -l "public class UIPanel" Assets/Stubs/NGUI/UIPanel.cs` returns the file
    - `grep -l "public class UIWidget" Assets/Stubs/NGUI/UIWidget.cs` returns the file
    - `grep -l "public class UICamera" Assets/Stubs/NGUI/UICamera.cs` returns the file
    - `grep -l "public static class NGUITools" Assets/Stubs/NGUI/NGUITools.cs` returns the file
    - `grep -l "public class EventDelegate" Assets/Stubs/NGUI/EventDelegate.cs` returns the file
    - `grep -c "namespace" Assets/Stubs/NGUI/*.cs` returns 0 for every file (global namespace)
    - `grep -l ": UIWidget" Assets/Stubs/NGUI/UISprite.cs` confirms inheritance (via UIBasicSprite)
    - `grep -l "public string text" Assets/Stubs/NGUI/UILabel.cs` confirms text property exists
    - `grep -l "public string spriteName" Assets/Stubs/NGUI/UISprite.cs` confirms spriteName exists
  </acceptance_criteria>
  <done>All 16 core NGUI type stubs exist in Assets/Stubs/NGUI/ with correct inheritance chains, properties, methods, and enums that the game code references. No namespace declarations. All types extend MonoBehaviour or another NGUI stub type.</done>
</task>

<task type="auto">
  <name>Task 2: Create NGUI interaction, layout, and tween stubs</name>
  <files>
    Assets/Stubs/NGUI/UIButton.cs,
    Assets/Stubs/NGUI/UICheckbox.cs,
    Assets/Stubs/NGUI/UIToggle.cs,
    Assets/Stubs/NGUI/UISlider.cs,
    Assets/Stubs/NGUI/UIProgressBar.cs,
    Assets/Stubs/NGUI/UIScrollBar.cs,
    Assets/Stubs/NGUI/UIInput.cs,
    Assets/Stubs/NGUI/UIPopupList.cs,
    Assets/Stubs/NGUI/UIGrid.cs,
    Assets/Stubs/NGUI/UITable.cs,
    Assets/Stubs/NGUI/UIScrollView.cs,
    Assets/Stubs/NGUI/UIDragScrollView.cs,
    Assets/Stubs/NGUI/UIWrapContent.cs,
    Assets/Stubs/NGUI/UIEventListener.cs,
    Assets/Stubs/NGUI/UITweener.cs,
    Assets/Stubs/NGUI/TweenPosition.cs,
    Assets/Stubs/NGUI/TweenRotation.cs,
    Assets/Stubs/NGUI/TweenScale.cs,
    Assets/Stubs/NGUI/TweenAlpha.cs,
    Assets/Stubs/NGUI/TweenColor.cs,
    Assets/Stubs/NGUI/UIPlayTween.cs,
    Assets/Stubs/NGUI/SpringPanel.cs,
    Assets/Stubs/NGUI/UISpriteAnimation.cs
  </files>
  <read_first>
    Run these grep commands to discover the exact members used for each type:
    - `grep -rn 'UIButton\.' Assets/Scripts/ | grep -oP 'UIButton\.\w+' | sort | uniq -c | sort -rn | head -15`
    - `grep -rn 'UICheckbox\.' Assets/Scripts/ | grep -oP 'UICheckbox\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UISlider\.' Assets/Scripts/ | grep -oP 'UISlider\.\w+' | sort | uniq -c | sort -rn | head -15`
    - `grep -rn 'UIScrollBar\.' Assets/Scripts/ | grep -oP 'UIScrollBar\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIInput\.' Assets/Scripts/ | grep -oP 'UIInput\.\w+' | sort | uniq -c | sort -rn | head -15`
    - `grep -rn 'UIGrid\.' Assets/Scripts/ | grep -oP 'UIGrid\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UITable\.' Assets/Scripts/ | grep -oP 'UITable\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIPopupList\.' Assets/Scripts/ | grep -oP 'UIPopupList\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIEventListener\.' Assets/Scripts/ | grep -oP 'UIEventListener\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UITweener\.' Assets/Scripts/ | grep -oP 'UITweener\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'TweenPosition\.\|TweenScale\.\|TweenAlpha\.\|TweenColor\.\|TweenRotation\.' Assets/Scripts/ | grep -oP 'Tween\w+\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'SpringPanel\.' Assets/Scripts/ | grep -oP 'SpringPanel\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIScrollView\.' Assets/Scripts/ | grep -oP 'UIScrollView\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIToggle\.' Assets/Scripts/ | grep -oP 'UIToggle\.\w+' | sort | uniq -c | sort -rn`
    - `grep -rn 'UIProgressBar\.' Assets/Scripts/ | grep -oP 'UIProgressBar\.\w+' | sort | uniq -c | sort -rn`
  </read_first>
  <action>
    Create stubs for all NGUI interaction, layout, and animation types. All in the GLOBAL namespace (no namespace declaration). Each file is a standalone .cs file.

    Interaction types:
    - UIButton : MonoBehaviour (has isEnabled, onClick, normalSprite, hoverSprite, pressedSprite, disabledSprite, tweenTarget, defaultColor, hover, pressed, disabledColor, state enum, current static)
    - UICheckbox : MonoBehaviour (has isChecked, startsChecked, checkSprite, checkAnimation, optionGroup, onStateChange EventDelegate.List, static current)
    - UIToggle : MonoBehaviour (has value, startsActive, activeSprite, activeAnimation, group, onChange List<EventDelegate>, static current, optionCanBeNone)
    - UISlider : UIProgressBar (has value, numberOfSteps, onValueChange List<EventDelegate>, static current, direction enum)
    - UIProgressBar : UIWidget (has value, foregroundWidget, backgroundWidget, thumb, numberOfSteps, onValueChange List<EventDelegate>, onChange List<EventDelegate>, current static, FillDirection enum)
    - UIScrollBar : UISlider (has value, barSize, alpha)
    - UIInput : MonoBehaviour (has value, label, maxChars, caretColor, selectionColor, inputType enum, validation enum, isSelected, onSubmit List<EventDelegate>, onChange List<EventDelegate>, KeyboardType enum, static current, InputType enum, Validation enum)
    - UIPopupList : MonoBehaviour (has items List<string>, value, isOpen, onValueChange, onChange List<EventDelegate>, textLabel, static current, Selection enum)
    - UIEventListener : MonoBehaviour (has onClick, onHover, onPress, onSelect, onScroll, onDrag, onDrop, onKey, onTooltip, onSubmit, onChange, static Get(GameObject))

    Layout types:
    - UIGrid : MonoBehaviour (has arrangement enum, maxPerLine, cellWidth, cellHeight, animateSmoothly, hideInactive, sorting, pivot, Reposition(), GetChildList(), Arrangement enum, Sorting enum)
    - UITable : MonoBehaviour (has columns, direction, sorting, hideInactive, keepWithinPanel, padding, Reposition(), Direction enum, Sorting enum)
    - UIScrollView : MonoBehaviour (has panel, currentMomentum, RestrictWithinBounds(), DisableSpring(), movement enum, dragEffect enum, Movement enum, DragEffect enum)
    - UIDragScrollView : MonoBehaviour (has scrollView)
    - UIWrapContent : MonoBehaviour (has itemSize, minIndex, maxIndex, onInitializeItem)
    - UISpriteAnimation : MonoBehaviour (has framesPerSecond, namePrefix, loop, isPlaying, RebuildSpriteList(), Play(), Pause(), ResetToBeginning())
    - UIPlayTween : MonoBehaviour (has tweenTarget, tweenGroup, trigger, playDirection, resetOnPlay, ifDisabledOnPlay)
    - SpringPanel : MonoBehaviour (has target, strength, static Begin(GameObject, Vector3, float))

    Tween types (inheritance: UITweener : MonoBehaviour):
    - UITweener : MonoBehaviour (has style enum, animationCurve, duration, delay, tweenGroup, onFinished List<EventDelegate>, tweenFactor, ignoreTimeScale, PlayForward(), PlayReverse(), ResetToBeginning(), SetStartToCurrentValue(), SetEndToCurrentValue(), Sample(), Style enum, Method enum)
    - TweenPosition : UITweener (has from, to Vector3, value Vector3, static Begin(GameObject, float, Vector3))
    - TweenRotation : UITweener (has from, to Vector3, static Begin(GameObject, float, Quaternion))
    - TweenScale : UITweener (has from, to Vector3, value Vector3, static Begin(GameObject, float, Vector3))
    - TweenAlpha : UITweener (has from, to float, value float, static Begin(GameObject, float, float))
    - TweenColor : UITweener (has from, to Color, value Color, static Begin(GameObject, float, Color))

    All methods return default values. All events/delegates are initialized to empty but non-null where the game code calls .Add() or .Remove() on them.
  </action>
  <verify>
    <automated>ls Assets/Stubs/NGUI/*.cs | wc -l</automated>
    Expected: at least 38 .cs files total (16 from Task 1 + 23 from Task 2, though some like UIProgressBar may be new)
  </verify>
  <acceptance_criteria>
    - `grep -l "public class UIButton" Assets/Stubs/NGUI/UIButton.cs` returns the file
    - `grep -l "public class UIEventListener" Assets/Stubs/NGUI/UIEventListener.cs` returns the file
    - `grep -l "public class UITweener" Assets/Stubs/NGUI/UITweener.cs` returns the file
    - `grep -l "public class TweenPosition" Assets/Stubs/NGUI/TweenPosition.cs` returns the file
    - `grep -l "public class UIGrid" Assets/Stubs/NGUI/UIGrid.cs` returns the file
    - `grep -l "public class UIScrollView" Assets/Stubs/NGUI/UIScrollView.cs` returns the file
    - `grep -l "static.*Get" Assets/Stubs/NGUI/UIEventListener.cs` confirms Get(GameObject) exists
    - `grep -l ": UITweener" Assets/Stubs/NGUI/TweenPosition.cs` confirms inheritance
    - `grep -c "namespace" Assets/Stubs/NGUI/UIButton.cs` returns 0 (global namespace)
    - `ls Assets/Stubs/NGUI/*.cs | wc -l` returns at least 38
  </acceptance_criteria>
  <done>All NGUI interaction, layout, and tween stubs exist with correct members, inheritance, and global namespace. Combined with Task 1, every NGUI type referenced by the 417+ game files has a corresponding stub.</done>
</task>

</tasks>

<verification>
1. Count stub files: `ls Assets/Stubs/NGUI/*.cs | wc -l` -- expect 38+
2. No namespace declarations: `grep -l "^namespace" Assets/Stubs/NGUI/*.cs` -- expect empty (no matches)
3. Core types present: `grep -rl "public class UILabel\|public class UISprite\|public class UIPanel\|public class UIButton\|public class UIWidget\|public class UICamera" Assets/Stubs/NGUI/ | wc -l` -- expect 6
4. Inheritance correct: `grep ": UIWidget\|: UIRect\|: UIBasicSprite\|: UITweener\|: UIProgressBar\|: UISlider\|: MonoBehaviour" Assets/Stubs/NGUI/*.cs | wc -l` -- expect 35+
</verification>

<success_criteria>
- Every NGUI type name found via `grep -roh 'UI[A-Z][A-Za-z]*' Assets/Scripts/` that is an actual NGUI type has a corresponding stub file
- All stubs are in the global namespace
- All stubs have correct MonoBehaviour inheritance chains
- All properties, methods, and enums referenced by game code exist in the stubs
</success_criteria>

<output>
After completion, create `.planning/phases/02-proprietary-plugin-stubs/02-01-SUMMARY.md`
</output>
