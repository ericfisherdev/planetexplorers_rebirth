---
phase: 09-platform-builds-persistence
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - Assets/Editor/BuildPipeline/RebuildBuildPlayer.cs
autonomous: false

requirements: [PLAT-01, PLAT-02, PLAT-03]

must_haves:
  truths:
    - "Linux standalone build launches, reaches main menu, and can start a game"
    - "Windows standalone build launches, reaches main menu, and can start a game"
    - "Save game from within standalone build writes files to disk"
    - "Load game from within standalone build restores state"
  artifacts:
    - path: "Assets/Editor/BuildPipeline/RebuildBuildPlayer.cs"
      provides: "Build script verified working with actual builds"
  key_links:
    - from: "Standalone executable"
      to: "GameConfig.GetPeUserDataPath()/GameSave/"
      via: "ArchiveMgr.Save writes to disk"
      pattern: "GetPeUserDataPath"
    - from: "Standalone executable"
      to: "EditorBuildSettings.scenes"
      via: "Build includes all required scenes"
      pattern: "GetEnabledScenes"
---

<objective>
Build standalone executables for Linux and Windows, then verify the full core loop: launch, main menu, start game, play, save, quit, reload, and confirm state persistence.

Purpose: This is the integration verification plan -- it confirms that the save/load system (Plan 01) and the build pipeline (Plan 02) work together in actual standalone builds. This is the final gate for v1 release readiness.

Output: Verified standalone builds on both platforms with confirmed save/load persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-platform-builds-persistence/09-01-SUMMARY.md
@.planning/phases/09-platform-builds-persistence/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build scene list and fix any missing scene references</name>
  <files>
    Assets/Editor/BuildPipeline/RebuildBuildPlayer.cs
  </files>
  <read_first>
    Assets/Editor/BuildPipeline/RebuildBuildPlayer.cs
    ProjectSettings/EditorBuildSettings.asset
  </read_first>
  <action>
1. Read ProjectSettings/EditorBuildSettings.asset and extract the list of scenes configured for build. The original game had 10 build scenes (from Phase 1 backup): GameStart, GameMainMenu, GameRoleCustom, GameStory, GameAdventure, GameBuild, CreationSystem, GameTraining, GameLobby, MLoginScene.

2. Verify that at minimum these scenes are required for single-player:
   - GameStart (bootstrap/splash)
   - GameMainMenu (main menu)
   - GameRoleCustom (character creation)
   - GameStory or GameAdventure (gameplay scenes)
   - GameBuild (building mode)
   - GameTraining (tutorial)

   The multiplayer scenes (GameLobby, MLoginScene, GameClient) can be excluded since we are single-player only.

3. If EditorBuildSettings.asset does not have scenes configured (which is likely since the project has been through migration), add a method to RebuildBuildPlayer.cs called `ValidateSceneList()` that:
   - Checks if EditorBuildSettings.scenes has any enabled entries
   - If empty, logs an error: "No scenes configured in Build Settings. Use File > Build Settings to add scenes."
   - Call ValidateSceneList() at the start of each build menu item, returning early if validation fails.

4. Add a menu item "Rebirth/Build/Validate Scene List" that logs all currently configured scenes and warns about any missing core scenes.
  </action>
  <verify>
    <automated>grep -c "ValidateSceneList\|EditorBuildSettings.scenes" Assets/Editor/BuildPipeline/RebuildBuildPlayer.cs</automated>
  </verify>
  <acceptance_criteria>
    - RebuildBuildPlayer.cs contains a ValidateSceneList method
    - Build methods call ValidateSceneList before proceeding
    - A "Validate Scene List" menu item exists
    - grep returns >= 3 (ValidateSceneList definition + calls + scene validation)
  </acceptance_criteria>
  <done>Build script validates scene configuration before building, preventing builds with missing scenes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build and verify standalone executables</name>
  <what-built>
    Complete build pipeline with scene validation. Plans 01 and 02 fixed save/load persistence and created the modern build script. This task verifies everything works end-to-end.
  </what-built>
  <how-to-verify>
    1. Open the project in Unity 6 editor
    2. Use File > Build Settings to verify scene list includes GameStart, GameMainMenu, and gameplay scenes
    3. Use Rebirth > Build > Linux x64 to produce a Linux standalone build
    4. Use Rebirth > Build > Windows x64 to produce a Windows standalone build
    5. Launch the Linux build:
       a. Verify it reaches the main menu
       b. Start a new game, move the player, interact with the world
       c. Save the game (manual save or wait for auto-save)
       d. Quit the game
       e. Re-launch, load the save, verify player position and world state are restored
       f. Check that save files exist under ~/Documents/PlanetExplorers/GameSave/ (or equivalent)
    6. Repeat step 5 on Windows:
       a. Verify save files exist under Documents\PlanetExplorers\GameSave\
    7. Report any crashes, missing assets, or broken save/load behavior
  </how-to-verify>
  <resume-signal>Type "approved" if both platforms build and the core loop works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Linux standalone build launches without crash
2. Windows standalone build launches without crash
3. Save files appear on disk after saving
4. Loading a save restores game state
5. No deprecated API warnings in build output
</verification>

<success_criteria>
- Linux x64 standalone build launches, reaches main menu, and plays through the core loop
- Windows x64 standalone build launches, reaches main menu, and plays through the core loop
- Save/load cycle preserves player position, inventory, and world state
- No crashes during build or runtime
</success_criteria>

<output>
After completion, create `.planning/phases/09-platform-builds-persistence/09-03-SUMMARY.md`
</output>
