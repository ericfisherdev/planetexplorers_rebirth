---
phase: 09-platform-builds-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/GameArchive/ArchiveMgr.cs
  - Assets/GameArchive/Archive.cs
  - Assets/GameArchive/ArchiveObj.cs
  - Assets/GameArchive/PeRecordWriter.cs
  - Assets/GameArchive/PeRecordReader.cs
  - Assets/GameArchive/ArchiveIndex.cs
  - Assets/GameArchive/SwapSpace.cs
  - Assets/GameArchive/ArchivableSingleton.cs
  - Assets/GameArchive/AutoArchiveRunner.cs
  - Assets/PeLauncher/PeArchiveMgrs.cs
  - Assets/Scripts/System/GameConfig.cs
  - Assets/Block45/Block45DataStructure/B45ChunkData.cs
  - Assets/Terrain/Voxelform2/Voxelform/Source/Scripts/Voxel/Block45/Block45Man.cs
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "Player can save game state and data is written to disk at GetPeUserDataPath()/GameSave/"
    - "Player can load a previously saved game and all registered ArchiveObj data is restored"
    - "Auto-save fires every 5 minutes on a rotating 3-slot cycle"
    - "Block45 voxel chunk data persists across save/load (tech debt TODO filled in)"
    - "Save path works on both Linux (~/.local/share or ~/Documents) and Windows (My Documents)"
  artifacts:
    - path: "Assets/GameArchive/ArchiveMgr.cs"
      provides: "Save/load orchestration with slot management"
      contains: "SaveArchive"
    - path: "Assets/GameArchive/Archive.cs"
      provides: "Binary archive read/write with versioned header and checksum"
      contains: "WriteToFile"
    - path: "Assets/GameArchive/AutoArchiveRunner.cs"
      provides: "Auto-save coroutine runner on application quit"
      contains: "AutoSave"
    - path: "Assets/Block45/Block45DataStructure/B45ChunkData.cs"
      provides: "Voxel chunk serialization"
      contains: "Serialize"
  key_links:
    - from: "Assets/GameArchive/ArchiveMgr.cs"
      to: "Assets/GameArchive/Archive.cs"
      via: "mCurArchive.WriteToFile / mCurArchive.LoadFromFile"
      pattern: "mCurArchive\\."
    - from: "Assets/PeLauncher/PeArchiveMgrs.cs"
      to: "Assets/GameArchive/ArchiveMgr.cs"
      via: "ArchiveMgr.Instance.Register"
      pattern: "ArchiveMgr\\.Instance\\.Register"
    - from: "Assets/GameArchive/AutoArchiveRunner.cs"
      to: "Assets/GameArchive/ArchiveMgr.cs"
      via: "ArchiveMgr.Instance.Save / QuitSave"
      pattern: "ArchiveMgr\\.Instance\\."
---

<objective>
Audit and fix the save/load persistence system so game state serializes to disk and restores correctly on both Linux and Windows.

Purpose: The existing Archive system (ArchiveMgr, Archive, PeRecordWriter/Reader, ArchivableSingleton pattern with ~20 registered subsystems) is already implemented with binary serialization. However, it has known tech debt (B45ChunkData save/load is stubbed with TODOs, Block45Man has TODO stubs at lines 188/193/425) and the save path logic needs verification for cross-platform correctness on Unity 6.

Output: A working save/load pipeline where ArchiveMgr.Save() writes all registered subsystems to disk and ArchiveMgr.Load() restores them, including voxel terrain chunk data that was previously stubbed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
<!-- Key types the executor needs from the Archive system -->

From Assets/GameArchive/ArchiveObj.cs:
```csharp
namespace Pathea {
    public interface ISerializable {
        void Serialize(PeRecordWriter w);
    }
    // ArchiveObj wraps key + ISerializable + yird flag + recordName
}
```

From Assets/GameArchive/ArchivableSingleton.cs:
```csharp
namespace Pathea {
    public abstract class ArchivableSingleton<T> : MonoLikeSingleton<T>, ISerializable where T : class, new() {
        protected abstract string GetArchiveKey();
        protected virtual bool GetYird() { return false; }
        protected abstract void WriteData(BinaryWriter bw);
        protected abstract void SetData(byte[] data);
    }
}
```

From Assets/GameArchive/ArchiveMgr.cs:
```csharp
namespace Pathea {
    public class ArchiveMgr : MonoLikeSingleton<ArchiveMgr> {
        public void Register(string key, ISerializable serializableObj, bool yird = false, string recordName = "world", bool saveFlagResetValue = true);
        public void Save(ESave eSave);
        public string Load(ESave eSave);
        public byte[] GetData(string key);
        public PeRecordReader GetReader(string key);
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix B45ChunkData save/load stubs and verify cross-platform save paths</name>
  <files>
    Assets/Block45/Block45DataStructure/B45ChunkData.cs
    Assets/Terrain/Voxelform2/Voxelform/Source/Scripts/Voxel/Block45/Block45Man.cs
    Assets/Scripts/System/GameConfig.cs
  </files>
  <read_first>
    Assets/Block45/Block45DataStructure/B45ChunkData.cs
    Assets/Terrain/Voxelform2/Voxelform/Source/Scripts/Voxel/Block45/Block45Man.cs
    Assets/Scripts/System/GameConfig.cs
    Assets/GameArchive/PeRecordWriter.cs
    Assets/GameArchive/PeRecordReader.cs
  </read_first>
  <action>
1. Read B45ChunkData.cs and locate the TODO stubs at lines 12-13. The class has existing IO infrastructure (BinaryReader/BinaryWriter patterns used elsewhere in the file). Implement Serialize/Deserialize methods that write/read the chunk's voxel byte data array. Follow the versioned serialization pattern used throughout the codebase: write CURRENT_VERSION int first, then data, and on read check version before deserializing.

2. Read Block45Man.cs and locate the TODO stubs at lines 188, 193, and 425. These are save/load entry points. Implement Export(PeRecordWriter w) to iterate all loaded B45ChunkData instances and serialize each one (keyed by chunk position). Implement Import(PeRecordReader r) to deserialize and restore chunk data. Follow the same pattern as VFVoxelTerrain.SaveLoad.Export/Import which is the sibling system already working.

3. In GameConfig.cs, verify GetPeUserDataPath() returns a valid cross-platform path:
   - On Linux: System.Environment.SpecialFolder.MyDocuments resolves to ~/Documents (or ~/ on some distros). Verify this works on Unity 6 Linux standalone. If MyDocuments returns empty string on Linux, add a fallback to use Environment.GetFolderPath(SpecialFolder.LocalApplicationData) or Application.persistentDataPath.
   - On Windows: MyDocuments works as expected.
   - Add a Debug.Log at GetPeUserDataPath() to log the resolved path on first access for debugging.

4. Ensure the save directory creation in ArchiveMgr.SaveArchive() handles the case where parent directories don't exist (use Directory.CreateDirectory which creates the full path including parents).
  </action>
  <verify>
    <automated>grep -n "TODO" Assets/Block45/Block45DataStructure/B45ChunkData.cs Assets/Terrain/Voxelform2/Voxelform/Source/Scripts/Voxel/Block45/Block45Man.cs | grep -i "save\|load\|import\|export" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep for "TODO.*save\|TODO.*load\|TODO.*import\|TODO.*export" in B45ChunkData.cs returns 0 matches
    - B45ChunkData.cs contains Serialize and Deserialize methods with version int header
    - Block45Man.cs Export/Import methods contain actual serialization logic (not empty/stubbed)
    - GameConfig.GetPeUserDataPath() has a Linux fallback for empty MyDocuments path
  </acceptance_criteria>
  <done>Block45 voxel chunk data has working serialization, and the save path resolves correctly on both platforms</done>
</task>

<task type="auto">
  <name>Task 2: Audit and harden the Archive pipeline for Unity 6 compatibility</name>
  <files>
    Assets/GameArchive/ArchiveMgr.cs
    Assets/GameArchive/Archive.cs
    Assets/GameArchive/AutoArchiveRunner.cs
    Assets/GameArchive/SwapSpace.cs
    Assets/GameArchive/PeRecordWriter.cs
    Assets/GameArchive/PeRecordReader.cs
    Assets/GameArchive/ArchiveIndex.cs
  </files>
  <read_first>
    Assets/GameArchive/ArchiveMgr.cs
    Assets/GameArchive/Archive.cs
    Assets/GameArchive/AutoArchiveRunner.cs
    Assets/GameArchive/SwapSpace.cs
    Assets/GameArchive/PeRecordWriter.cs
    Assets/GameArchive/PeRecordReader.cs
    Assets/GameArchive/ArchiveIndex.cs
  </read_first>
  <action>
1. Audit all GameArchive/*.cs files for Unity 6 API compatibility issues:
   - UnityEngine.Profiling.Profiler.BeginSample/EndSample -- verify these still exist in Unity 6 (they do, no change needed).
   - BinaryReader/BinaryWriter -- standard .NET, no Unity dependency, should work unchanged.
   - FileStream/MemoryStream -- standard .NET, no Unity dependency.
   - Check for any deprecated Unity 5 APIs that were not caught in Phase 1.

2. In AutoArchiveRunner.cs:
   - The static `Start()` method shadows MonoBehaviour.Start(). Rename it to `StartRunner()` or use `new` keyword to suppress the warning. This was likely a latent bug in Unity 5 that Unity 6 may warn about.
   - Verify OnApplicationQuit() is still called in Unity 6 standalone builds (it is, per Unity docs).

3. In ArchiveMgr.cs:
   - The Delete method has an empty catch block (line 363: `catch{}`). Add `catch(System.Exception e) { Debug.LogWarning("[ArchiveMgr.Delete] " + e.Message); }` to avoid silent failures.
   - The array sizing `mArchiveHeaders = new Archive.Header[ESave.Max - ESave.Min]` -- verify the enum arithmetic works correctly (ESave.Max=28, ESave.Min=0, so 28 slots for 3 auto + 20 user + New).

4. In SwapSpace.cs -- read and verify it handles file operations safely. Check for any hardcoded path separators (\ vs /) that would break on Linux. Replace any `\\` path concatenation with Path.Combine().

5. In PeRecordWriter.cs and PeRecordReader.cs -- verify they use standard BinaryWriter/BinaryReader wrappers without platform-specific code.

6. In Archive.cs -- the Header.Read() method rejects archives where version != CurrentVersion (line 98-101). This is correct for forward-incompatible saves but means old saves are rejected. Leave this as-is (it's the original behavior) but add a comment noting this design choice.
  </action>
  <verify>
    <automated>grep -rn "catch{}" Assets/GameArchive/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep for "catch{}" in Assets/GameArchive/ returns 0 (no empty catch blocks)
    - AutoArchiveRunner.cs does not have a method signature conflict with MonoBehaviour.Start
    - No hardcoded backslash path separators in SwapSpace.cs (grep for "\\\\" returns 0)
    - All GameArchive files compile without Unity 6 deprecation warnings
  </acceptance_criteria>
  <done>The entire Archive pipeline is audited for Unity 6 compatibility, empty catch blocks are fixed, path handling is cross-platform safe, and no deprecated APIs remain</done>
</task>

</tasks>

<verification>
1. Grep for TODO stubs in Block45 save/load code returns 0 results
2. Grep for empty catch blocks in GameArchive returns 0 results
3. GameConfig.GetPeUserDataPath() has Linux fallback logic
4. All ArchiveMgr subsystem registrations in PeArchiveMgrs.cs have matching WriteData/SetData implementations
</verification>

<success_criteria>
- B45ChunkData has implemented Serialize/Deserialize (not stubbed)
- Block45Man has implemented Export/Import (not stubbed)
- GameConfig save path works on Linux and Windows
- Archive pipeline has no empty catch blocks and no hardcoded Windows paths
- AutoArchiveRunner has no method name conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/09-platform-builds-persistence/09-01-SUMMARY.md`
</output>
