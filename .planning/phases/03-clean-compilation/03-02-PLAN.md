---
phase: 03-clean-compilation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/PeLauncher/PeGameMgr.cs
  - Assets/Scripts/System/GameConfig.cs
  - Assets/Scripts/GameNetwork/NetworkInterface.cs
  - Assets/Scripts/GameNetwork/Player/PlayerNetwork.cs
  - Assets/Scripts/GameNetwork/GameClient/GameClientNetwork.cs
  - Assets/Scripts/GameNetwork/GameClient/GameClientLobby.cs
  - Assets/Scripts/GameNetwork/LocalTest.cs
autonomous: true
requirements: [CORE-04]

must_haves:
  truths:
    - "PeGameMgr.IsSingle returns true by default at startup"
    - "PeGameMgr.IsMulti returns false by default at startup"
    - "GameConfig.IsMultiMode returns false by default"
    - "NetworkInterface.IsClient returns false in single-player (no network connection)"
    - "All code paths guarded by IsMulti/IsMultiMode safely skip in single-player without crashing"
  artifacts:
    - path: "Assets/PeLauncher/PeGameMgr.cs"
      provides: "Single-player default game mode initialization"
      contains: "EPlayerType.Single"
    - path: "Assets/Scripts/System/GameConfig.cs"
      provides: "IsMultiMode returns false in single-player"
    - path: "Assets/Scripts/GameNetwork/NetworkInterface.cs"
      provides: "Network interface compiles and defaults to offline behavior"
  key_links:
    - from: "Assets/PeLauncher/PeGameMgr.cs"
      to: "Assets/Scripts/System/GameConfig.cs"
      via: "GameConfig.IsMultiMode delegates to PeGameMgr.IsMulti"
      pattern: "Pathea\\.PeGameMgr\\.IsMulti"
    - from: "Assets/Scripts/GameNetwork/NetworkInterface.cs"
      to: "uLink stubs"
      via: "uLink.Network.isClient, uLink.NetworkView"
      pattern: "uLink\\.Network\\.isClient"
---

<objective>
Wire networking code for single-player operation so that all multiplayer code paths safely no-op.

Purpose: The codebase has ~100+ files with multiplayer branching via `PeGameMgr.IsMulti`, `GameConfig.IsMultiMode`, `NetworkInterface.IsClient`, and uLink `isMine`/`isServer` checks. Phase 2 stubs define the uLink types, but the game logic still needs to default to single-player mode so these branches route correctly at runtime. Without this, the game will either try to connect to a network or crash on null network views.

Output: PeGameMgr defaults to single-player mode. Network stubs return safe single-player values. No live network calls occur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key network/game mode interfaces from the codebase -->

From Assets/PeLauncher/PeGameMgr.cs:
```csharp
public static bool IsSingle { get { return mPlayerType == EPlayerType.Single; } }
public static bool IsMulti { get { return mPlayerType == EPlayerType.Multiple || mPlayerType == EPlayerType.Tutorial; } }
// mPlayerType is set during game start flow
```

From Assets/Scripts/System/GameConfig.cs:
```csharp
public static bool IsMultiMode { get { return Pathea.PeGameMgr.IsMulti; } }
```

From Assets/Scripts/GameNetwork/NetworkInterface.cs:
```csharp
public static bool IsClient { get { return uLink.Network.isClient && uLink.Network.status == NetworkStatus.Connected && uLink.Network.peerType == uLink.NetworkPeerType.Client; } }
public bool IsOwner { get { return IsClient && _netView != null && _netView.isOwner; } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force single-player defaults in PeGameMgr and verify GameConfig routing</name>
  <files>
    Assets/PeLauncher/PeGameMgr.cs
    Assets/Scripts/System/GameConfig.cs
  </files>
  <read_first>
    Assets/PeLauncher/PeGameMgr.cs
    Assets/Scripts/System/GameConfig.cs
  </read_first>
  <action>
In `PeGameMgr.cs`:
1. Find where `mPlayerType` is declared and initialized. It should be a static field of type `EPlayerType`.
2. Set its default value to `EPlayerType.Single` so that `IsSingle` returns true and `IsMulti` returns false by default, before any game start flow runs.
3. Find the `IsMulti` property getter. Verify it checks against `EPlayerType.Multiple` (and possibly `Tutorial`). Ensure the default state means `IsMulti` is false.
4. Search for any static constructor or initialization that might set `mPlayerType` to a multiplayer value. If found, ensure it defaults to Single.

In `GameConfig.cs`:
1. Verify `IsMultiMode` already delegates to `Pathea.PeGameMgr.IsMulti` — if so, no changes needed (it will inherit the single-player default).
2. Check for any other multiplayer flags or network-related constants that should default to offline values.

Do NOT remove any multiplayer code. Only ensure the default state is single-player. The multiplayer code paths should remain intact but be unreachable when `IsMulti` is false.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -n "mPlayerType" Assets/PeLauncher/PeGameMgr.cs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "mPlayerType.*=.*EPlayerType.Single" in PeGameMgr.cs returns a match (default initialization)
    - PeGameMgr.IsSingle returns true when mPlayerType is at its default value
    - PeGameMgr.IsMulti returns false when mPlayerType is at its default value
    - GameConfig.IsMultiMode delegates to PeGameMgr.IsMulti (no independent multiplayer state)
  </acceptance_criteria>
  <done>PeGameMgr defaults to EPlayerType.Single. GameConfig.IsMultiMode returns false. All ~50+ files checking IsMulti/IsMultiMode will safely take the single-player branch.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure NetworkInterface and PlayerNetwork compile and default to offline single-player behavior</name>
  <files>
    Assets/Scripts/GameNetwork/NetworkInterface.cs
    Assets/Scripts/GameNetwork/Player/PlayerNetwork.cs
    Assets/Scripts/GameNetwork/GameClient/GameClientNetwork.cs
    Assets/Scripts/GameNetwork/GameClient/GameClientLobby.cs
    Assets/Scripts/GameNetwork/LocalTest.cs
  </files>
  <read_first>
    Assets/Scripts/GameNetwork/NetworkInterface.cs
    Assets/Scripts/GameNetwork/Player/PlayerNetwork.cs
    Assets/Scripts/GameNetwork/GameClient/GameClientNetwork.cs
    Assets/Scripts/GameNetwork/GameClient/GameClientLobby.cs
  </read_first>
  <action>
The uLink stubs from Phase 2 define types like `uLink.MonoBehaviour`, `uLink.NetworkView`, `uLink.Network`, `uLink.BitStream`, etc. This task ensures the game's networking layer compiles against those stubs and behaves safely in single-player.

For `NetworkInterface.cs`:
1. Read the full file. It extends `uLink.MonoBehaviour` and uses `uLink.NetworkView`, `uLink.BitStream`, `uLink.NetworkMessageInfo`.
2. Verify it compiles against the Phase 2 uLink stubs. The `IsClient` property checks `uLink.Network.isClient` — the stub should return false, making `IsClient` false, which means `IsOwner` and `IsProxy` also return false.
3. If there are initialization paths that call into uLink (e.g., `uLink.Network.Connect`), they should be guarded by `GameConfig.IsMultiMode` or `PeGameMgr.IsMulti`. If any initialization is NOT guarded, add a guard: `if (!GameConfig.IsMultiMode) return;`
4. Check `_netView` assignment. If it does `GetComponent<uLink.NetworkView>()` in Awake/Start without a null check, it will return null in single-player (no uLink.NetworkView component on the GameObject). Add null guards around `_netView` usage if missing.

For `PlayerNetwork.cs`:
1. Read the file (it's large, ~2545 lines across partial classes). Focus on initialization (Awake, Start, initialization methods).
2. Find `mainPlayerId` and `mainPlayer` static fields. These need to have sensible defaults for single-player.
3. Check for any RPC registration or uLink initialization. These should be guarded by multiplayer checks.
4. If `PlayerNetwork` is used by single-player code (via `PlayerNetwork.mainPlayer` or `PlayerNetwork.mainPlayerId`), ensure those accessors return safe values when networking is not active.

For `GameClientNetwork.cs` and `GameClientLobby.cs`:
1. Read each file. These are the primary network connection classes.
2. They should already be guarded by multiplayer mode checks, but verify.
3. If any `Connect()` or `Join()` calls are not guarded, add `if (!PeGameMgr.IsMulti) return;` guards at the top of those methods.

For `LocalTest.cs`:
1. Read the file. This appears to be a local testing helper.
2. Ensure it compiles and doesn't try to establish connections automatically.

Key principle: Do NOT delete networking code. Add guards and null checks so single-player mode never hits network code paths. The stubs handle type resolution; this task handles runtime safety.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "uLink.Network.Connect\|uLink.Network.InitializeServer" Assets/Scripts/GameNetwork/ --include="*.cs" | grep -v "IsMulti\|IsMultiMode\|//" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - No unguarded uLink.Network.Connect or uLink.Network.InitializeServer calls exist outside of IsMulti/IsMultiMode guards
    - NetworkInterface.IsClient returns false when uLink stubs return default values
    - PlayerNetwork.mainPlayer and mainPlayerId have safe default values (null/0)
    - GameClientNetwork and GameClientLobby connection methods are guarded by multiplayer mode checks
  </acceptance_criteria>
  <done>All networking code compiles against uLink stubs and defaults to offline single-player behavior. No live network calls occur when PeGameMgr.IsMulti is false.</done>
</task>

</tasks>

<verification>
- PeGameMgr.mPlayerType defaults to EPlayerType.Single
- GameConfig.IsMultiMode returns false at startup
- NetworkInterface, PlayerNetwork, GameClientNetwork, GameClientLobby all compile against uLink stubs
- No unguarded network connection calls exist
</verification>

<success_criteria>
All networking code compiles and routes through single-player stubs. PeGameMgr defaults to single-player mode. No multiplayer code path is reachable by default.
</success_criteria>

<output>
After completion, create `.planning/phases/03-clean-compilation/03-02-SUMMARY.md`
</output>
