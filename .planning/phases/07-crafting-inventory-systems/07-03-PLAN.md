---
phase: 07-crafting-inventory-systems
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs
  - Assets/Scripts/LootItem/LootItemMgr.cs
  - Assets/Scripts/Items/ItemDrop/ItemDrop.cs
  - Assets/Scripts/Items/ItemDrop/ItemDropData.cs
  - Assets/Scripts/Items/ItemDrop/ItemDropMousePick.cs
  - Assets/Scripts/Items/ItemDrop/ItemDropPeEntity.cs
  - Assets/Scripts/MousePicker/MousePickableLootItem.cs
  - Assets/Scripts/PeEntity/Cmpt/Motion/PEAction/Action_Live.cs
autonomous: true
requirements:
  - GAME-02
must_haves:
  truths:
    - "Replicator.Formula.Mgr loads crafting recipes from the SQLite synthesis_skill table"
    - "Replicator.Run consumes materials from inventory and creates product items"
    - "Replicator.StartReplicate begins a timed crafting process that produces items on completion"
    - "Player can pick up LootItem drops from the world into inventory"
    - "Crafted items appear in PlayerPackageCmpt inventory after replication completes"
  artifacts:
    - path: "Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs"
      provides: "Replicator with formula loading, material checking, timed crafting, and item creation"
      contains: "class Replicator"
    - path: "Assets/Scripts/LootItem/LootItemMgr.cs"
      provides: "World item drop management and pickup"
      contains: "class LootItemMgr"
    - path: "Assets/Scripts/Items/ItemDrop/ItemDrop.cs"
      provides: "Item drop spawning and droppable item list"
      contains: "class ItemDrop"
  key_links:
    - from: "Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs"
      to: "Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs"
      via: "IHandler.CreateItem/DeleteItem delegates to mPackage"
      pattern: "mPackage\\.package|mPackage\\.Destory|mPackage\\.Add"
    - from: "Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs"
      to: "Assets/Scripts/Database/LocalDatabase.cs"
      via: "Formula.Mgr.LoadData reads synthesis_skill table"
      pattern: "ReadFullTable.*synthesis_skill"
    - from: "Assets/Scripts/LootItem/LootItemMgr.cs"
      to: "Assets/Scripts/Items/ItemManager.cs"
      via: "ItemMgr.Instance.Get(id) to resolve dropped item objects"
      pattern: "ItemMgr\\.Instance\\.Get"
---

<objective>
Make the replication crafting system functional (recipe loading, material consumption, item production) and enable item pickup from world drops.

Purpose: Crafting is a core gameplay loop requirement -- players need to convert gathered materials into usable items. Item pickup completes the item acquisition loop.
Output: Replicator loads recipes from SQLite, crafts items into inventory. LootItemMgr pickup adds items to player inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

@.planning/phases/07-crafting-inventory-systems/07-01-SUMMARY.md

@Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs
@Assets/Scripts/LootItem/LootItemMgr.cs
@Assets/Scripts/Items/ItemDrop/ItemDrop.cs
@Assets/Scripts/Items/ItemDrop/ItemDropData.cs
@Assets/Scripts/Items/ItemDrop/ItemDropMousePick.cs
@Assets/Scripts/MousePicker/MousePickableLootItem.cs
@Assets/Scripts/PeEntity/Cmpt/Motion/PEAction/Action_Live.cs

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs:
```csharp
public class ReplicatorCmpt : PeCmpt, Replicator.IHandler
{
    public Replicator replicator { get; }
    // IHandler bridges to PlayerPackageCmpt:
    int IHandler.ItemCount(int itemId) => mPackage.package.GetCount(itemId);
    bool IHandler.DeleteItem(int itemId, int count) => mPackage.Destory(itemId, count);
    bool IHandler.CreateItem(int itemId, int count) => mPackage.Add(itemId, count);
}

public class Replicator
{
    public class Formula { int id; int productItemId; float timeNeed; short m_productItemCount; List<Material> materials; }
    public class Formula.Mgr : MonoLikeSingleton<Mgr> { /* loads from synthesis_skill table */ }
    public interface IHandler { int ItemCount(int); bool DeleteItem(int, int); bool CreateItem(int, int); bool HasEnoughPackage(int, int); }
    public bool Run(int formulaId, int count);  // instant craft
    public bool StartReplicate(int formulaID, int count);  // timed craft
    public void UpdateReplicate();  // called each frame
    public bool AddFormula(int formulaId);  // learn a recipe
}
```

From Assets/Scripts/LootItem/LootItemMgr.cs:
```csharp
public class LootItemMgr : ArchivableSingleton<LootItemMgr>
{
    Dictionary<int, LootItemData> m_Datas;
    // Creates world items, manages pickup proximity
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Replicator formula loading and crafting pipeline</name>
  <files>
    Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs
    Assets/Scripts/Database/LocalDatabase.cs
  </files>
  <action>
Audit the complete crafting pipeline from recipe loading to item production:

1. Read ReplicatorCmpt.cs fully (already partially read). Focus on:
   - Replicator.Formula.Mgr.LoadData() -- reads SQLite "synthesis_skill" table. Verify column names match: ID, object_ID, num_each_product, need_time, material1..material6 (6 material pairs: itemId + count). Ensure SqliteDataReader ordinals resolve correctly.
   - Replicator.AddFormula(formulaId) -- adds a known recipe. Verify KnownFormula stores the formula reference.
   - Replicator.Run(formulaId, count) -- instant crafting: checks MaxProductCount (material availability), calls DeleteMaterial then CreateItem via IHandler. Verify the full chain.
   - Replicator.StartReplicate/UpdateReplicate -- timed crafting: starts a RunningReplicate, decrements leftCount each tick when runningTime exceeds timeNeed. Verify the timer math and item creation on completion.

2. Verify ReplicatorCmpt.IHandler implementation:
   - ItemCount delegates to mPackage.package.GetCount(itemId) -- requires Plan 01's PlayerPackageCmpt
   - DeleteItem delegates to mPackage.Destory(itemId, count)
   - CreateItem delegates to mPackage.Add(itemId, count)
   - HasEnoughPackage delegates to mPackage.package.CanAdd(itemId, count)

3. Verify ReplicatorCmpt.OnUpdate:
   - Calls replicator.UpdateReplicate() each frame
   - References GameUI.Instance.mSkillWndCtrl -- this is NGUI UI code that will be stubbed. Ensure the RandomMapConfig.useSkillTree check gates this properly, and if the UI stub returns null, it does not crash. Add a null guard if needed.

4. Verify Serialize/Deserialize on Replicator:
   - Known formulas are saved (id + flag)
   - Running replicate state is saved (formulaID, counts, runningTime)
   - Deserialization restores state correctly

5. Fix any issues:
   - If GameConfig.IsMultiMode branches call PlayerNetwork.mainPlayer.RequestMergeSkill, ensure those stubs no-op for single-player
   - If MessageBox_N.ShowOkBox (NGUI stub) crashes, ensure it no-ops
   - If PELocalization.GetString returns null for missing localization IDs, ensure callers handle null
   - If the synthesis_skill SQLite table does not exist or has different column names, add graceful error handling

6. Ensure that after calling Replicator.Run(formulaId, 1):
   - Materials are removed from the player's inventory
   - The product item is created and added to inventory
   - The player can query their inventory and see the new item
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "synthesis_skill" Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs && grep -rn "bool Run\|StartReplicate\|UpdateReplicate" Assets/Scripts/PeEntity/Cmpt/ReplicatorCmpt.cs</automated>
  </verify>
  <done>Replicator.Formula.Mgr loads recipes from SQLite synthesis_skill table. Replicator.Run consumes materials and creates products via IHandler. StartReplicate/UpdateReplicate implements timed crafting correctly. Serialize/Deserialize preserves crafting state. No crashes from NGUI or network stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix item pickup from world drops (LootItemMgr)</name>
  <files>
    Assets/Scripts/LootItem/LootItemMgr.cs
    Assets/Scripts/Items/ItemDrop/ItemDrop.cs
    Assets/Scripts/Items/ItemDrop/ItemDropData.cs
    Assets/Scripts/Items/ItemDrop/ItemDropMousePick.cs
    Assets/Scripts/Items/ItemDrop/ItemDropPeEntity.cs
    Assets/Scripts/MousePicker/MousePickableLootItem.cs
    Assets/Scripts/PeEntity/Cmpt/Motion/PEAction/Action_Live.cs
  </files>
  <action>
Audit the item pickup pipeline from world drops to player inventory:

1. Read LootItemMgr.cs fully. Understand:
   - Constructor creates a root GameObject with UIPanel (NGUI stub -- ensure AddComponent<UIPanel>() does not crash)
   - Loads the LootItem prefab from Resources via Resources.Load("Prefab/LootItem/LootItem")
   - m_Datas dictionary stores LootItemData (item instance IDs + world positions)
   - Item generation based on proximity (GenerateSqrDis = 48*48)
   - Pickup based on proximity (SqrFetchRange = 4*4)
   - RemoveTime = 93600f (26 hours) for despawn

2. Read ItemDrop.cs, ItemDropData.cs, ItemDropMousePick.cs, ItemDropPeEntity.cs:
   - ItemDrop implements IItemDrop and IDroppableItemList
   - ItemDropData defines drop tables (what items drop from what sources)
   - ItemDropMousePick handles mouse-click pickup
   - ItemDropPeEntity handles entity death drops

3. Read MousePickableLootItem.cs -- the MonoBehaviour on world loot items that makes them clickable/pickable.

4. Read Action_PickUpItem in Action_Live.cs (around line 467). This is the PEAction that plays the pickup animation and transfers the item to inventory.

5. Fix any issues:
   - LootItemMgr constructor calls AddComponent<UIPanel>() -- this is an NGUI type. Ensure the stub UIPanel works as a no-op MonoBehaviour. If it fails, replace with a comment and remove the UIPanel requirement.
   - Resources.Load("Prefab/LootItem/LootItem") -- this prefab may not exist. Add null check and log warning if missing. The LootItemMgr should still function for programmatic item creation even without the visual prefab.
   - If MousePickableLootItem references NGUI components for item labels/icons, ensure those stub no-op
   - If ItemDropPeEntity references missing entity components, add null guards
   - Ensure that when a pickup is triggered (either by proximity or click), the item is: removed from LootItemMgr.m_Datas, added to PlayerPackageCmpt via Add(ItemObject)

6. Verify LootItemMgr serialization (ArchivableSingleton pattern) -- ensure save/load preserves world drops that have not yet been picked up.

The key outcome: items dropped in the world can be picked up and appear in the player's inventory.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "class LootItemMgr" Assets/Scripts/LootItem/LootItemMgr.cs && grep -rn "class ItemDrop " Assets/Scripts/Items/ItemDrop/ItemDrop.cs && grep -rn "Action_PickUpItem" Assets/Scripts/PeEntity/Cmpt/Motion/PEAction/Action_Live.cs</automated>
  </verify>
  <done>LootItemMgr manages world item drops without crashing (NGUI stub UIPanel handled). ItemDrop system creates pickable items from death drops and world spawns. Action_PickUpItem transfers items from world to player inventory. LootItemMgr serialization preserves un-picked-up items across save/load.</done>
</task>

</tasks>

<verification>
- Replicator.Formula.Mgr.Instance loads recipes from synthesis_skill SQLite table
- Replicator.Run(formulaId, count) removes materials and creates product items in inventory
- StartReplicate/UpdateReplicate handles timed crafting without crashes
- LootItemMgr does not crash on initialization (NGUI stubs handled)
- Item pickup transfers items from world to player inventory
- Both systems serialize/deserialize correctly
</verification>

<success_criteria>
- Crafting recipes load from SQLite database
- Instant crafting (Run) consumes materials and produces items
- Timed crafting (StartReplicate/UpdateReplicate) completes after timeNeed seconds
- World item drops can be picked up into player inventory
- All crafting and pickup state persists across save/load
- Single-player mode (no network calls) functions correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-crafting-inventory-systems/07-03-SUMMARY.md`
</output>
