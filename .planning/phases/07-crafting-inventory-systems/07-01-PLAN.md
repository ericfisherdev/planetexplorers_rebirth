---
phase: 07-crafting-inventory-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Items/ItemData.cs
  - Assets/Scripts/Items/ItemManager.cs
  - Assets/Scripts/Items/ItemObj.cs
  - Assets/Scripts/Items/ItemPak/ItemPackage.cs
  - Assets/Scripts/Items/ItemPak/SlotList.cs
  - Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs
  - Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs
  - Assets/Scripts/Database/LocalDatabase.cs
autonomous: true
requirements:
  - GAME-03
must_haves:
  truths:
    - "ItemMgr loads item prototype data from the SQLite database on initialization"
    - "ItemObject instances can be created from prototype IDs and hold component data (Equip, ReplicatorFormula, etc.)"
    - "PlayerPackageCmpt adds and removes items, reports counts, and serializes/deserializes correctly"
    - "ItemPackage manages slot-based storage with capacity limits and stack merging"
  artifacts:
    - path: "Assets/Scripts/Items/ItemManager.cs"
      provides: "ItemMgr singleton loading item prototypes from SQLite, creating/destroying ItemObject instances"
      contains: "class ItemMgr"
    - path: "Assets/Scripts/Items/ItemPak/ItemPackage.cs"
      provides: "Slot-based inventory container with Add/Remove/GetCount operations"
      contains: "class ItemPackage"
    - path: "Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs"
      provides: "Player entity inventory component bridging PackageCmpt to ItemPackage"
      contains: "class PlayerPackageCmpt"
  key_links:
    - from: "Assets/Scripts/Items/ItemManager.cs"
      to: "Assets/Scripts/Database/LocalDatabase.cs"
      via: "SQLite query for item prototype table"
      pattern: "LocalDatabase.*ReadFullTable.*item"
    - from: "Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs"
      to: "Assets/Scripts/Items/ItemPak/ItemPackage.cs"
      via: "mPackage field of type PlayerPackage"
      pattern: "PlayerPackage"
    - from: "Assets/Scripts/Items/ItemObj.cs"
      to: "Assets/Scripts/Items/ItemData.cs"
      via: "ItemObject extends ItemSample which holds ItemProto reference"
      pattern: "class ItemObject.*ItemSample"
---

<objective>
Wire the item data loading pipeline and inventory storage so that item prototypes load from SQLite, ItemObject instances can be created, and the player inventory (PlayerPackageCmpt) supports add/remove/query operations.

Purpose: Without functional item data and inventory, neither equipment nor crafting can work. This is the foundation layer.
Output: ItemMgr loads prototypes, ItemPackage stores items in slots, PlayerPackageCmpt integrates with the entity system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

@Assets/Scripts/Items/ItemData.cs
@Assets/Scripts/Items/ItemManager.cs
@Assets/Scripts/Items/ItemObj.cs
@Assets/Scripts/Items/ItemPak/ItemPackage.cs
@Assets/Scripts/Items/ItemPak/SlotList.cs
@Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs
@Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs
@Assets/Scripts/Database/LocalDatabase.cs

<interfaces>
<!-- Key types the executor needs to understand -->

From Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs:
```csharp
public abstract class PackageCmpt : PeCmpt, EquipmentCmpt.Receiver
{
    public abstract bool Add(ItemObject item, bool isNew = false);
    public abstract bool Remove(ItemObject item);
    public abstract bool Contain(ItemObject item);
    public abstract int GetItemCount(int protoId);
    public abstract bool Destory(int protoId, int count);
    public abstract bool Add(int protoId, int count);
}
```

From Assets/Scripts/Items/ItemObj.cs:
```csharp
public class ItemObject : ItemSample, IEnumerable<Cmpt>
{
    public int instanceId { get; }
    public void Init();
    public T GetCmpt<T>() where T : Cmpt;
}
```

From Assets/Scripts/Items/ItemPak/ItemPackage.cs:
```csharp
namespace ItemAsset.PackageHelper {
    public static class PackageAccessor {
        public static bool ContainsItem(this ItemPackage package, int prototypeId);
    }
}
public class ItemPackage { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix ItemMgr data loading pipeline from SQLite</name>
  <files>
    Assets/Scripts/Items/ItemManager.cs
    Assets/Scripts/Items/ItemData.cs
    Assets/Scripts/Items/ItemObj.cs
    Assets/Scripts/Database/LocalDatabase.cs
  </files>
  <action>
Audit the ItemMgr class (MonoLikeSingleton) to ensure its initialization path works:

1. Read ItemManager.cs fully -- understand how ItemMgr.Instance initializes and loads item prototype data. The class reads from SQLite tables via LocalDatabase. Trace the full init chain: what tables does it read? What data populates ItemProto objects?

2. Read ItemData.cs fully -- understand ItemProto, ItemSample, and the static data loading (ItemProto.LoadData reads from SQLite "item_data" or similar table). Ensure all references resolve (no missing stub types that would cause null returns).

3. Verify ItemObj.cs component system -- ItemObject.GetCmpt<T>() must find components like Equip, ReplicatorFormula, etc. The Cmpt subclasses are defined in ItemObj.cs (Equip at line ~1505, ReplicatorFormula at line ~675). Ensure these component types load their data from item prototype correctly.

4. Check LocalDatabase.cs -- ensure ReadFullTable and query methods return valid SqliteDataReader objects. If the i18n.db or game database paths are hardcoded, verify they resolve at runtime. LocalDatabase is the bottleneck -- if it fails, nothing loads.

5. Fix any issues found:
   - Stub types that return null instead of empty defaults (e.g., NGUI stubs that ItemMgr might reference for UI notifications -- ensure they no-op rather than crash)
   - Missing table columns (if SQLite schema doesn't match code expectations, add graceful fallbacks)
   - Null reference chains in ItemProto static loading

6. Ensure ItemMgr.Instance.CreateItem(protoId) successfully creates an ItemObject with the correct components attached, and ItemMgr.Instance.Get(instanceId) retrieves it.

Do NOT touch UI code. Do NOT modify EquipmentCmpt or ReplicatorCmpt -- those are handled in other plans.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "class ItemMgr" Assets/Scripts/Items/ItemManager.cs && grep -rn "class ItemProto" Assets/Scripts/Items/ItemData.cs && grep -rn "class ItemObject" Assets/Scripts/Items/ItemObj.cs</automated>
  </verify>
  <done>ItemMgr singleton initializes without errors, reads item prototype data from SQLite, and creates ItemObject instances with attached Cmpt components. No null-reference crashes in the item data loading pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix inventory storage (ItemPackage and PlayerPackageCmpt)</name>
  <files>
    Assets/Scripts/Items/ItemPak/ItemPackage.cs
    Assets/Scripts/Items/ItemPak/SlotList.cs
    Assets/Scripts/Items/ItemPak/ShortCut.cs
    Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs
    Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs
  </files>
  <action>
Audit the inventory storage pipeline to ensure items can be added, removed, and queried:

1. Read ItemPackage.cs fully -- understand the slot-based storage model. ItemPackage uses SlotList internally. Understand how Add(ItemObject), Remove, GetCount(protoId), CanAdd, and Export/Import work.

2. Read SlotList.cs fully -- this is the underlying slot array. Understand slot capacity, stacking behavior (items with same protoId stack up to a max per slot), and index management.

3. Read PlayerPackageCmpt.cs fully -- this bridges the abstract PackageCmpt to a concrete PlayerPackage instance. Verify:
   - package property initializes PlayerPackage with PkgCapacity (420 slots)
   - Add(ItemObject) delegates to package correctly
   - Add(int protoId, int count) creates items via ItemMgr and adds them
   - Destory(int protoId, int count) removes items
   - GetItemCount(int protoId) queries package
   - Serialize/Deserialize round-trips correctly (Export/Import on PlayerPackage)

4. Read ShortCut.cs -- verify the shortcut bar slot list works (maps quickbar slots to inventory items).

5. Fix any issues:
   - If PlayerPackage constructor references missing types, stub them
   - If SlotList Add/Remove has off-by-one or null-check issues, fix them
   - Ensure the Serialize/Deserialize cycle preserves item instance IDs correctly
   - If PlayerPackageCmpt.Add(int protoId, int count) calls ItemMgr.Instance.CreateItem(), ensure that chain works

6. Verify the PackageCmpt.Start() method wires mItemReciver to EquipmentCmpt correctly (this is the bridge that lets equipment removal return items to inventory).

Do NOT modify EquipmentCmpt itself. Do NOT touch UI code.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "class PlayerPackageCmpt" Assets/Scripts/PeEntity/Cmpt/PlayerPackageCmpt.cs && grep -rn "class ItemPackage" Assets/Scripts/Items/ItemPak/ItemPackage.cs && grep -rn "bool Add" Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs</automated>
  </verify>
  <done>PlayerPackageCmpt.Add(itemObject) stores items in the underlying ItemPackage. PlayerPackageCmpt.GetItemCount(protoId) returns correct counts. Serialize/Deserialize round-trips preserve all inventory state. PackageCmpt.Start() wires the EquipmentCmpt.Receiver interface.</done>
</task>

</tasks>

<verification>
- ItemMgr.Instance initializes and loads item prototype data from SQLite without crashes
- ItemMgr.Instance.CreateItem(protoId) returns a valid ItemObject with components
- PlayerPackageCmpt.Add(itemObject) succeeds and GetItemCount returns the correct value
- PlayerPackageCmpt.Serialize/Deserialize preserves inventory contents
- No null-reference exceptions in the item data or inventory pipelines
</verification>

<success_criteria>
- ItemProto static data loads from SQLite tables
- ItemObject instances are created with correct component set (Equip, ReplicatorFormula, etc.)
- ItemPackage Add/Remove/GetCount operations function correctly
- PlayerPackageCmpt integrates with the entity component system
- Serialization round-trips preserve all item and inventory state
</success_criteria>

<output>
After completion, create `.planning/phases/07-crafting-inventory-systems/07-01-SUMMARY.md`
</output>
