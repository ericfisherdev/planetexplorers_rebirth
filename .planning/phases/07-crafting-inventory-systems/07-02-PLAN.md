---
phase: 07-crafting-inventory-systems
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs
  - Assets/Scripts/PeEntity/Cmpt/Motion/Motion_Equip.cs
  - Assets/Scripts/Items/ItemObj.cs
  - Assets/Scripts/NPC/Follower/EquipItemMgr.cs
  - Assets/Scripts/SkillSystem/SkEntity.cs
  - Assets/Scripts/SkillSystem/SkBuffs.cs
autonomous: true
requirements:
  - GAME-03
must_haves:
  truths:
    - "Player can equip a weapon or armor item via EquipmentCmpt.PutOnEquipment"
    - "Equipping an item applies stat buffs to the entity SkEntity via the Equip component"
    - "Unequipping an item removes stat buffs and returns the item to inventory"
    - "Equipment position bitmask prevents conflicting slots (cannot equip two helmets)"
    - "Equipment serialization preserves equipped item instance IDs across save/load"
  artifacts:
    - path: "Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs"
      provides: "Equipment management: equip/unequip with buff application, model management, durability"
      contains: "class EquipmentCmpt"
    - path: "Assets/Scripts/PeEntity/Cmpt/Motion/Motion_Equip.cs"
      provides: "Equipment motion handling for held items"
      contains: "class Motion_Equip"
    - path: "Assets/Scripts/Items/ItemObj.cs"
      provides: "Equip component on ItemObject holding equipPos, buff data, durability"
      contains: "class Equip : Cmpt"
  key_links:
    - from: "Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs"
      to: "Assets/Scripts/SkillSystem/SkEntity.cs"
      via: "Equip.AddBuff(mSkEntity) and Equip.RemoveBuff(mSkEntity)"
      pattern: "AddBuff.*SkEntity|RemoveBuff.*SkEntity"
    - from: "Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs"
      to: "Assets/Scripts/Items/ItemObj.cs"
      via: "itemObj.GetCmpt<ItemAsset.Equip>() to access equipment properties"
      pattern: "GetCmpt<.*Equip>"
    - from: "Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs"
      to: "Assets/Scripts/PeEntity/Cmpt/PackageCmpt.cs"
      via: "mItemReciver (Receiver interface) to return items on unequip"
      pattern: "mItemReciver"
---

<objective>
Make the equipment system functional so that equipping weapons/armor applies stat modifications via the skill system, and unequipping returns items to inventory.

Purpose: Equipment affecting stats is a core gameplay success criterion -- players need visible gameplay impact from equipping gear.
Output: EquipmentCmpt.PutOnEquipment/TakeOffEquipment work end-to-end with buff application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

@Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs
@Assets/Scripts/PeEntity/Cmpt/Motion/Motion_Equip.cs
@Assets/Scripts/Items/ItemObj.cs
@Assets/Scripts/SkillSystem/SkEntity.cs
@Assets/Scripts/SkillSystem/SkBuffs.cs
@Assets/Scripts/SkillSystem/SkAttribs.cs
@Assets/Scripts/NPC/Follower/EquipItemMgr.cs

<interfaces>
<!-- Key types the executor needs to understand -->

From Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs:
```csharp
public class EquipmentCmpt : PeCmpt, IPeMsg
{
    public interface Receiver {
        bool CanAddItemList(List<ItemObject> items);
        void AddItemList(List<ItemObject> items);
    }
    public Receiver mItemReciver;
    public bool PutOnEquipment(ItemObject itemObj, bool addToReceiver = true, Receiver receiver = null, bool netRequest = false);
    public bool TakeOffEquipment(ItemObject itemObj, bool addToReceiver = true, Receiver receiver = null, bool netRequest = false);
    public EquipType mEquipType;
}
```

From Assets/Scripts/Items/ItemObj.cs (Equip component, ~line 1505):
```csharp
public class Equip : Cmpt
{
    public int equipPos;      // bitmask for slot position
    public int sex;            // gender restriction
    public void AddBuff(SkEntity skEntity);
    public void RemoveBuff(SkEntity skEntity);
    public void ExpendAttackDurability(SkEntity skEntity);
    public void ExpendDefenceDurability(SkEntity skEntity, float damage);
}
```

From Assets/Scripts/Items/ItemData.cs:
```csharp
public enum EquipType { Null=0, Spade=1, Mine=2, Axe=4, Sword=8, ... Clothes=1<<19, Trousers=1<<20 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix EquipmentCmpt equip/unequip and buff pipeline</name>
  <files>
    Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs
    Assets/Scripts/Items/ItemObj.cs
    Assets/Scripts/SkillSystem/SkEntity.cs
    Assets/Scripts/SkillSystem/SkBuffs.cs
    Assets/Scripts/SkillSystem/SkAttribs.cs
  </files>
  <action>
Audit the equipment-to-stats pipeline end-to-end:

1. Read EquipmentCmpt.cs fully (already partially read -- focus on PutOnEquipment and ApplyItemEff methods). Trace the equip flow:
   - PutOnEquipment receives ItemObject
   - Gets ItemAsset.Equip component via itemObj.GetCmpt<ItemAsset.Equip>()
   - Checks gender match, slot conflicts (equipPos bitmask AND)
   - Removes conflicting equipment to mItemReciver
   - Calls ApplyItemEff which calls equip.AddBuff(mSkEntity)
   - Dispatches change event

2. Read the Equip class in ItemObj.cs (starts around line 1505). Understand:
   - How equipPos bitmask maps to EquipType enum
   - How AddBuff/RemoveBuff work -- they should apply SkBuff instances to the SkEntity
   - What data the Equip component reads from ItemProto (protoData)
   - How durability (ExpendAttackDurability/ExpendDefenceDurability) works

3. Read SkEntity.cs -- understand MountBuff and CancelBuffById methods. These are the buff application/removal entry points called by equipment. Ensure:
   - MountBuff accepts buff IDs from equipment data
   - Buff effects modify SkAttribs (the attribute system: HP, attack, defense, etc.)
   - CancelBuffById properly reverses buff effects

4. Read SkBuffs.cs -- understand how buffs store modifiers and apply/remove them from attributes.

5. Fix any issues:
   - If Equip.AddBuff references buff IDs that come from item prototype data, ensure those IDs exist in the skill data tables (or at minimum don't crash on missing data -- graceful fallback)
   - If mSkEntity is null at equip time (Start() not yet called), add null guard
   - If GameConfig.IsMultiMode branches reference PlayerNetwork stubs, ensure those stubs no-op correctly for single-player
   - If BiologyViewCmpt (mViewCmpt) methods for model attachment crash on missing models, add null guards -- visual model attachment is Phase 8 (UI) concern, but the code path must not crash

6. Verify the EquipSetData.GetSuitSetEffect and SuitSetData.GetSuitSetEffect calls in CheckEXBuffs -- these provide set bonus buffs. Ensure they handle empty equipment lists and missing set data gracefully.

Do NOT implement visual equipment models (AddModel/CreateModel may reference missing assets -- ensure they fail gracefully). Do NOT modify PackageCmpt or PlayerPackageCmpt (Plan 01 handles those).
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "PutOnEquipment\|TakeOffEquipment\|ApplyItemEff\|RemoveItemEff" Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs && grep -rn "AddBuff\|RemoveBuff" Assets/Scripts/Items/ItemObj.cs</automated>
  </verify>
  <done>EquipmentCmpt.PutOnEquipment applies stat buffs via Equip.AddBuff(SkEntity). TakeOffEquipment reverses buffs via RemoveBuff. Slot conflict resolution works via equipPos bitmask. No null-reference crashes on equip/unequip. GameConfig.IsMultiMode branches no-op safely for single-player.</done>
</task>

<task type="auto">
  <name>Task 2: Verify equipment serialization and Motion_Equip integration</name>
  <files>
    Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs
    Assets/Scripts/PeEntity/Cmpt/Motion/Motion_Equip.cs
    Assets/Scripts/NPC/Follower/EquipItemMgr.cs
  </files>
  <action>
Ensure equipment state persists across save/load and the motion system handles equipped items:

1. Read EquipmentCmpt.Serialize/Deserialize (lines ~733-758). The serialization writes a version ID, item count, and instance IDs for each equipped item. Deserialization reads those IDs and calls ItemMgr.Instance.Get(id) to recover ItemObjects, storing them in m_InitItems for deferred equip in Start(). Verify:
   - Version handling is correct
   - ItemMgr.Instance.Get(id) returns valid objects (depends on Plan 01's item loading)
   - m_InitItems deferred equip in Start() calls PutOnEquipment correctly

2. Read Motion_Equip.cs -- this handles animation states for equipped items (weapon held, tool active). Verify:
   - SetEquipment(PEEquipment, bool) method exists and handles null gracefully
   - If PEEquipment references are stubs (visual models not yet available), the motion system does not crash
   - IPeMsg.OnMsg handler for equipment-related messages works

3. Read EquipItemMgr.cs -- this manages NPC equipment logic. Verify:
   - Does not crash when NPCs have no equipment
   - Works with the same EquipmentCmpt interface

4. Fix any issues:
   - If Motion_Equip references animation controller states that don't exist yet, add null guards
   - If PEEquipment (the visual equipment MonoBehaviour) is not available (no prefabs loaded), ensure CreateModel in EquipmentCmpt logs a warning but does not crash
   - If EquipItemMgr assumes NPC entity has specific components, add null checks

The goal is robustness: equipment logic works for stats even when visual models are unavailable.
  </action>
  <verify>
    <automated>cd /home/esfisher/dev/planetexplorers_rebirth && grep -rn "Serialize\|Deserialize" Assets/Scripts/PeEntity/Cmpt/EquipmentCmpt.cs && grep -rn "class Motion_Equip" Assets/Scripts/PeEntity/Cmpt/Motion/Motion_Equip.cs</automated>
  </verify>
  <done>EquipmentCmpt Serialize/Deserialize preserves equipped item instance IDs. Deserialized items are re-equipped via deferred PutOnEquipment in Start(). Motion_Equip handles missing visual models gracefully. No crashes on save/load cycles with equipped items.</done>
</task>

</tasks>

<verification>
- EquipmentCmpt.PutOnEquipment applies buffs to SkEntity without crashing
- EquipmentCmpt.TakeOffEquipment removes buffs and returns items to PackageCmpt receiver
- Slot conflict detection via equipPos bitmask prevents double-equipping
- Serialize/Deserialize round-trips preserve all equipped item state
- Missing visual models (PEEquipment) do not cause crashes
</verification>

<success_criteria>
- PutOnEquipment returns true and applies stat buffs for valid equipment items
- TakeOffEquipment returns true and reverses stat buffs
- Equipment slot conflicts are properly detected and resolved
- Save/load preserves equipped items
- Single-player mode (GameConfig.IsMultiMode == false) paths all function correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-crafting-inventory-systems/07-02-SUMMARY.md`
</output>
